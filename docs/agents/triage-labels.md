# Triage Labels

Use `needs-info` when required information is missing.
Use `ready-for-human` when a human decision is required.
Use `approved`, `ready-to-merge`, and `merged` only as the delivery workflow permits.
