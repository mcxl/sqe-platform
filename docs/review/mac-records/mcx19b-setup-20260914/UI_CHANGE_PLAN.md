# Release Screen Revision

Date: 14 September 2026.

## Authority And Baseline

The user asked how to fix the visible app screen after confirming the phone pilot opened. Their earlier instruction permits unbounded current-task work with all approvals. No paid membership, Codemagic, live client information, Production, merge, or unrelated changes.

Baseline: 7dc7cee7654ae8cc002da54324c3d1475a8494e6. The original Mac checkout remains unchanged. Revision work uses LOCAL_HOME/Developer/sqe-platform-release-layout on codex/mcx19b-release-layout.

The user supplied a physical iPhone screenshot showing the release fixture. The same source was installed, launched through devicectl, and visually confirmed by the user. Native launch evidence is phone-pilot-launch-trusted.json. Screenshot: phone-before-layout.jpg in the private Windows record directory.

## Observed Issues And Scope

- The debug fixture path bypasses the normal RootView content padding. Make their margins consistent.
- The visible light label is debug metadata. Permit an explicit manual-preview option to omit it. Automated audit runs keep their default metadata. This option is not a method for passing an audit.
- Release fields need clear visual groups, a usable hierarchy, and compact copy controls.
- Keep Dynamic Type, complete values, exact field-specific copy labels, the clipboard contract, safe notices, and existing callbacks.
- Keep the required fictional handling label visible. Do not change raw dates, status values, network behaviour, credential handling, or public interfaces.

Owned application presentation: Views.swift and ACEClientAppApp.swift. Native verification also needs the retained standalone copyConfirmation ScrollView correction in DebugScenario.swift. Its earlier baseline-fail/candidate-pass/reversal-fail evidence is recorded in prior-evidence-inventory.json.

The UI test file receives the two retained standalone noConclusion/noActions selectors and their helper. It also checks that copy confirmation becomes fully visible and passes an unrestricted audit. These are focused checks of changed behaviour. No audit filter, assertion removal, new dependency, or repository tool is added. Other retained diagnostic overlays remain unapplied.

## Checks Before Acceptance

1. Inspect the complete source diff and identify exact source hashes.
2. Build the revision on the verified Mac and simulator.
3. Inspect actual release screenshots in light mode, dark mode, and maximum accessibility text. Check margins, wrapping, groups, controls, and visible confirmation.
4. Run existing affected native interactions and unrestricted accessibility audits for release, no conclusion, no actions, and copy confirmation. Retrieve exact failures and attachments before any corrective change. Review the selected harness before execution.
5. Retain existing failing baseline evidence. A new passing run alone does not resolve its cause.
6. Obtain fresh read-only review after parent diff inspection and native checks.
7. Install the private revision on the phone and obtain a visual comparison. Record manual confirmation separately from automated acceptance.

This is focused revision coverage. It does not settle the complete specification matrix or establish final MCX19B acceptance. Prior five matrix passes remain historical evidence; this revision creates a new candidate. The full matrix, live server, signed Keychain, and eight VoiceOver journeys remain outstanding.

## Runtime And Evidence

Use the proven generic simulator build-for-testing and generated xctestrun route. No test-plan or snapshot framework replacement. Retrieve native result bundles directly with xcresulttool. The prior deliberate failure/pass gate is retained and passed.

The total duration is not yet established. Native UI audit execution on this host remains to be measured. Keep focused runs separate from any final complete suite.

Private records and raw results remain under the existing ace-private and Windows Codex record folders. No claim of fixed or accepted UI is permitted while required native checks are missing or failing.
