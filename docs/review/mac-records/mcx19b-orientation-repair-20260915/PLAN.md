# ACE Orientation Repair And 22-Case Pilot

## Result And Authority

Complete the 22-case simulator pilot on one clean candidate. Correct the orientation-check cancellation without weakening native audits, observed settings or layout checks. 
Your existing unbounded authority applies. Stop correction of one fault after two failed corrective attempts. Stop each coverage run at its first unresolved failure. No paid services, new dependencies, phone testing, private-service changes, push, merge or 836-case expansion belong to this work.

## Verified Resources

Use the Mac repository at LOCAL_HOME/Developer/sqe-platform-release-layout and its tools/ace_ios_local.py runner. Retained failure evidence is at LOCAL_HOME/ace-private/mcx19b-complete-pilot-20260915. Store current evidence in this private directory. Verify tools, candidate, retained build hashes and devices before dependent work.

The starting commit is 464671562cfb0249e859ffc8905a8f8c5ef17e55. Low Power Mode is now off. 
## Implementation And Verification

1. Diagnose: run only the unchanged failed settings check with the retained build after the power change. Record observed settings, native assertions, timing and restoration. A pass alone does not prove a correction.
2. Correct and prove: remove the demonstrated outer-timer cancellation from frame reading. Keep independent in-app checks. Record actual frame values and separate reading errors from mismatches. Limit source changes to the existing UI-test file unless evidence requires an authorised runner correction. Prove portrait and landscape pass and a deliberate mismatch fails. Inspect the complete diff and obtain required current reviews. Refresh affected evidence gates before freezing the candidate.
3. Accept: run all 22 cases, comprising 20 audit cases and two layout cases. Require 36 native audit invocations, exact settings, no skips and complete artifacts. Inspect distinct images at original resolution. Verify hashes, private retrieval, restoration and 30-day retention.

## Control And Delivery

The first diagnostic may take five to ten minutes, based on prior command times. Full-pilot duration is not established. Native builds and tests consume most execution time. Separate measured execution, collection, review and waiting.

Deliver the plan, result and evidence links on Windows; keep bulk evidence on the Mac. Preserve historical failures and unresolved causes explicitly.

Correcting the orientation check and completing the verified 22-case pilot is your /goal; work autonomously within these boundaries until it is complete.


## Focused Shutdown Correction

The905004f native gate retained the expected native failure and normal release pass. All native settings restores passed. Final cleanup failed because restore_boot_state issued shutdown after the batch had already shut down iPhone17. The native diagnostic confirmed exit149 and CoreSimulator.SimError405 with Shutdown both before and after.

Correct only the runner helper and regression tests. Preserve real shutdown and unknown-state failures. Review and commit the focused patch, repeat the native failure/pass gate, inspect images and restoration, then execute all22pilot cases on the clean candidate. Keep the earlier gate failed. Existing unbounded authority and two-failed-corrections-per-fault limit remain.


## 15 September 2026, Scroll-Margin Correction

The eef4b16 pilot ended at09:07:02UTC with exit2. Its first batch passed sign-in and release before noConclusion failed. No complete batch or pilot case is accepted. All original simulator settings and boot states were restored.

Native test: testCoverageBatch at ACEClientAppUITests.swift:373. Expected: no unrestricted audit findings. Actual: Contrast failed for SwiftUI.AccessibilityNode, label Release Details. The callback frame was x32,y67.67,width121.33,height20.33. The native image shows the heading partly clipped at the top scroll edge. This is distinct from earlier timeout and runner faults.

All27retained PNGs were inspected through10distinct hashes. Native bundles, failure text and video remain private on the Mac.

Correction attempt1 will move ReleaseView default margins inside its ScrollView content. Production and DEBUG release screens must use the same margin placement. Initial margins, non-release views, card padding, text, buttons and unrestricted native assertions must remain unchanged. This is a rendered-clipping correction, not a new screen design.

Before expansion, run the noConclusion case on iPhone17 with default text, portrait/light and all three settings off. Retain initial and scrolled images, both native audit outcomes, observed settings and restoration. This is diagnostic evidence and earns no pilot credit. A pass alone does not prove the full audit mechanism. Review images and the exact patch before freezing a new candidate and starting the22-case pilot. Stop further batches on any unresolved failure.
