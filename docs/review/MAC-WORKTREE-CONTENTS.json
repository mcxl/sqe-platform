{
  "worktrees": [
    {
      "worktree": "LOCAL_HOME/Developer/sqe-platform",
      "HEAD": "7dc7cee7654ae8cc002da54324c3d1475a8494e6",
      "branch": "refs/heads/codex/mcx-19-live-evidence-harness",
      "diff": "",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/ace-private/mcx19b-implementation-20260914/pilot-preparation-20260915/failure-probe-source",
      "HEAD": "493e632b2640fb12b54bf709719b0320cadd899a",
      "detached": true,
      "diff": "",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/ace-private/mcx19b-orientation-repair-20260915/08-copy-confirmation-diagnosis/copy-confirmation-audit-worktree",
      "HEAD": "6718b437b10a51eeee0e5c6b3918dc2d5e052260",
      "detached": true,
      "diff": "",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/ace-private/mcx19b-orientation-repair-20260915/09-signin-scroll-diagnosis/capture-negative-worktree",
      "HEAD": "c8b10875658e5fbd6a66dea1fbcec9bb4a2f5b0d",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\nindex 29f56b9..9b217b9 100644\n--- a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n+++ b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n@@ -121,6 +121,7 @@ final class ACEClientAppUITests: XCTestCase {\n                               \"Observed settings differ from requested settings for \\(coverageCase.id): \\(observed.payload)\") else { return false }\n         var screenshots: [String] = []\n         captureCoverageViewport(in: app, caseID: coverageCase.id, viewport: \"initial\", screenshots: &screenshots)\n+        XCTFail(\"ACE_CAPTURE_GATE_INTENTIONAL_FAILURE after retained initial viewport\")\n         var audits = 0\n         if coverageCase.audit {\n             guard runCoverageAudit(in: app, scenario: coverageCase.scenario, viewport: \"initial\") else { return false }\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/ace-private/mcx19b-orientation-repair-20260915/09-signin-scroll-diagnosis/capture-worktree",
      "HEAD": "e4264d27aeff1fc53accf84b39e48249ecb89b5e",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\nindex 25d0d9e..29f56b9 100644\n--- a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n+++ b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n@@ -377,7 +377,7 @@ final class ACEClientAppUITests: XCTestCase {\n     private func captureCoverageViewport(in app: XCUIApplication, caseID: String, viewport: String,\n                                          screenshots: inout [String]) {\n         let name = \"Coverage \\(caseID) viewport \\(viewport)\"\n-        addScreenshot(of: app, named: name)\n+        addScreenshot(of: app, named: name, snapshot: XCUIScreen.main.screenshot())\n         screenshots.append(name)\n     }\n \n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/ace-private/mcx19b-orientation-repair-20260915/09-signin-scroll-diagnosis/implementation-worktree",
      "HEAD": "15e4363c2ac074ceeea71599d1f2b64f67446487",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\nindex b11bc9c..25d0d9e 100644\n--- a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n+++ b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n@@ -337,14 +337,25 @@ final class ACEClientAppUITests: XCTestCase {\n             scrollUntilVisible(control, in: app, field: field)\n             return isFullyVisible(control, in: scrollView)\n         }\n+        let collectionView = app.collectionViews.firstMatch\n+        if collectionView.exists {\n+            return scrollControlUntilFullyVisible(control, in: collectionView)\n+        }\n         let table = app.tables.firstMatch\n         guard table.exists else { return false }\n+        return scrollControlUntilFullyVisible(control, in: table)\n+    }\n+\n+    private func scrollControlUntilFullyVisible(_ control: XCUIElement, in scrollView: XCUIElement) -> Bool {\n         for _ in 0..<16 {\n-            if isFullyVisible(control, in: table) { return true }\n+            if isFullyVisible(control, in: scrollView) { return true }\n             let controlFrame = control.exists ? control.frame : .zero\n-            drag(table, upward: !control.exists || controlFrame == .zero || controlFrame.maxY > table.frame.maxY)\n+            drag(\n+                scrollView,\n+                upward: !control.exists || controlFrame == .zero || controlFrame.maxY > scrollView.frame.maxY\n+            )\n         }\n-        return isFullyVisible(control, in: table)\n+        return isFullyVisible(control, in: scrollView)\n     }\n \n     private func verifyActionTarget(_ button: XCUIElement, name: String) -> Bool {\n@@ -358,6 +369,7 @@ final class ACEClientAppUITests: XCTestCase {\n     }\n \n     private func isFullyVisibleInApp(_ element: XCUIElement, app: XCUIApplication) -> Bool {\n+        guard element.exists else { return false }\n         let frame = element.frame\n         return frame.width > 0 && frame.height > 0 && app.frame.contains(frame)\n     }\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/ace-private/mcx19b-orientation-repair-20260915/10-target-size-diagnosis/measurement-fix-worktree",
      "HEAD": "c8b10875658e5fbd6a66dea1fbcec9bb4a2f5b0d",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\nindex 29f56b9..0fe7961 100644\n--- a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n+++ b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n@@ -359,9 +359,10 @@ final class ACEClientAppUITests: XCTestCase {\n     }\n \n     private func verifyActionTarget(_ button: XCUIElement, name: String) -> Bool {\n-        requireCoverage(button.exists && button.isHittable, \"\\(name) must be reachable\")\n-            && requireCoverage(isAtLeast44Points(button.frame.width), \"\\(name) width is below 44 points\")\n-            && requireCoverage(isAtLeast44Points(button.frame.height), \"\\(name) height is below 44 points\")\n+        guard requireCoverage(button.exists && button.isHittable, \"\\(name) must be reachable\") else { return false }\n+        let frame = button.frame\n+        return requireCoverage(isAtLeast44Points(frame.width), actionTargetDiagnostic(name, dimension: \"width\", frame: frame))\n+            && requireCoverage(isAtLeast44Points(frame.height), actionTargetDiagnostic(name, dimension: \"height\", frame: frame))\n     }\n \n     private func coverageElement(in app: XCUIApplication, labeled label: String) -> XCUIElement {\n@@ -968,30 +969,34 @@ final class ACEClientAppUITests: XCTestCase {\n     private func assertMinimumActionTargets(in app: XCUIApplication, file: StaticString = #filePath, line: UInt = #line) {\n         // Off-screen elements need scrolling before their hit area can be measured.\n         for button in app.buttons.allElementsBoundByIndex where button.isHittable {\n+            let frame = button.frame\n             XCTAssertTrue(\n-                isAtLeast44Points(button.frame.width),\n-                actionTargetDiagnostic(button.label, dimension: \"width\", measurement: button.frame.width),\n+                isAtLeast44Points(frame.width),\n+                actionTargetDiagnostic(button.label, dimension: \"width\", frame: frame),\n                 file: file,\n                 line: line\n             )\n             XCTAssertTrue(\n-                isAtLeast44Points(button.frame.height),\n-                actionTargetDiagnostic(button.label, dimension: \"height\", measurement: button.frame.height),\n+                isAtLeast44Points(frame.height),\n+                actionTargetDiagnostic(button.label, dimension: \"height\", frame: frame),\n                 file: file,\n                 line: line\n             )\n         }\n     }\n \n+    private static let targetMeasurementPrecision: CGFloat = 0.000_000_001\n+\n     private func isAtLeast44Points(_ measurement: CGFloat) -> Bool {\n         let minimum: CGFloat = 44\n-        // XCTest can report a 44-point SwiftUI target eight ULP below 44.\n-        return measurement >= minimum || minimum - measurement <= minimum.ulp * 8\n+        // This test-harness precision policy does not model XCTest arithmetic.\n+        guard measurement.isFinite else { return false }\n+        return measurement >= minimum - Self.targetMeasurementPrecision\n     }\n \n-    private func actionTargetDiagnostic(_ label: String, dimension: String, measurement: CGFloat) -> String {\n+    private func actionTargetDiagnostic(_ label: String, dimension: String, frame: CGRect) -> String {\n         let minimum: CGFloat = 44\n-        return \"\\(label) \\(dimension): measured \\(measurement), minimum \\(minimum), tolerance \\(minimum.ulp * 8) (eight ULP)\"\n+        return \"\\(label) \\(dimension): measured width \\(frame.width), height \\(frame.height), minimum \\(minimum), precision \\(Self.targetMeasurementPrecision) points\"\n     }\n \n     private func assertReleaseContrastProbeFramesAreStable(in app: XCUIApplication) {\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/ace-private/mcx19b-orientation-repair-20260915/10-target-size-diagnosis/target-size-probe-worktree",
      "HEAD": "f5bd9c43cb1bf6dfb45986b9e5df420be28ad9d2",
      "detached": true,
      "diff": "",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/ace-private/mcx19b-orientation-repair-20260915/11-settings-toggle-diagnosis/settings-fix-worktree",
      "HEAD": "c0e53738d15f677f9fd69b4d1eeba9f228ac51d1",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\nindex 0fe7961..54db2a6 100644\n--- a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n+++ b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n@@ -415,6 +415,18 @@ final class ACEClientAppUITests: XCTestCase {\n         return true\n     }\n \n+    private enum NativeSwitchAction {\n+        case succeed\n+        case tap\n+        case fail\n+    }\n+\n+    private static func nativeSwitchAction(current: Bool?, expected: Bool, taps: Int) -> NativeSwitchAction {\n+        guard let current else { return .fail }\n+        if current == expected { return .succeed }\n+        return taps < 2 ? .tap : .fail\n+    }\n+\n     private func setNativeAccessibilityToggle(_ label: String, path: [String], enabled: Bool, in settings: XCUIApplication) -> Bool {\n         guard returnToSettingsRoot(settings) else {\n             addScreenshot(of: settings, named: \"Accessibility settings missing root\")\n@@ -428,30 +440,60 @@ final class ACEClientAppUITests: XCTestCase {\n             XCTFail(message)\n             return false\n         }\n-        let toggle = settings.switches[label]\n-        guard toggle.waitForExistence(timeout: 5), let current = nativeSwitchValue(toggle) else {\n-            addScreenshot(of: settings, named: \"Accessibility settings missing \\(label)\")\n-            XCTFail(\"Missing Settings control: \\(label)\")\n-            return false\n-        }\n-        // Settings exposes the whole row as a Switch. A centre tap misses the visible switch.\n-        print(\"ACE_NATIVE_SWITCH \\(label) before=\\(current) expected=\\(enabled) frame=\\(toggle.frame)\")\n-        if current != enabled {\n-            toggle.coordinate(withNormalizedOffset: CGVector(dx: 0.9, dy: 0.5)).tap()\n-        }\n-        let observationStart = ProcessInfo.processInfo.systemUptime\n-        let changed = XCTNSPredicateExpectation(predicate: NSPredicate { _, _ in self.nativeSwitchValue(toggle) == enabled }, object: nil)\n-        let waitResult = XCTWaiter.wait(for: [changed], timeout: 5)\n-        // Read the current value before diagnostics; the wait can expire while Settings rebuilds its controls.\n-        let actual = nativeSwitchValue(settings.switches[label])\n-        let observationSeconds = ProcessInfo.processInfo.systemUptime - observationStart\n-        print(\"ACE_NATIVE_SWITCH_RESULT \\(label) wait=\\(waitResult.rawValue) seconds=\\(observationSeconds) expected=\\(enabled) actual=\\(String(describing: actual))\")\n-        guard actual == enabled else {\n-            addScreenshot(of: settings, named: \"Accessibility settings \\(label) did not change\")\n-            XCTFail(\"Settings control \\(label): expected \\(enabled), actual \\(String(describing: actual))\")\n-            return false\n+        var taps = 0\n+        var firstAttemptActual: Bool?\n+        while true {\n+            let toggle = settings.switches[label]\n+            guard toggle.waitForExistence(timeout: 5) else {\n+                addScreenshot(of: settings, named: \"Accessibility settings missing \\(label)\")\n+                XCTFail(\"Missing Settings control: \\(label)\")\n+                return false\n+            }\n+            let current = nativeSwitchValue(toggle)\n+            switch Self.nativeSwitchAction(current: current, expected: enabled, taps: taps) {\n+            case .succeed:\n+                emitJSON(marker: \"ACE_NATIVE_SWITCH_ATTEMPT\", payload: [\n+                    \"action\": \"alreadyRequested\", \"attempt\": taps, \"current\": current.map { $0 as Any } ?? NSNull(),\n+                    \"expected\": enabled, \"label\": label\n+                ])\n+                return true\n+            case .fail:\n+                let failure = current == nil ? \"unreadable\" : \"exhausted\"\n+                addScreenshot(of: settings, named: \"Accessibility settings \\(label) \\(failure)\")\n+                XCTFail(\"Settings control \\(label): \\(failure), expected \\(enabled), current \\(String(describing: current)), taps \\(taps)\")\n+                return false\n+            case .tap:\n+                let nextAttempt = taps + 1\n+                let frame = toggle.frame\n+                emitJSON(marker: \"ACE_NATIVE_SWITCH_ATTEMPT\", payload: [\n+                    \"action\": \"tap\", \"attempt\": nextAttempt, \"before\": current.map { $0 as Any } ?? NSNull(),\n+                    \"expected\": enabled, \"frame\": [\"height\": frame.height, \"width\": frame.width, \"x\": frame.minX, \"y\": frame.minY],\n+                    \"label\": label\n+                ])\n+                // Settings exposes the whole row as a Switch. A centre tap misses the visible switch.\n+                toggle.coordinate(withNormalizedOffset: CGVector(dx: 0.9, dy: 0.5)).tap()\n+                taps = nextAttempt\n+                let observationStart = ProcessInfo.processInfo.systemUptime\n+                let changed = XCTNSPredicateExpectation(predicate: NSPredicate { _, _ in self.nativeSwitchValue(toggle) == enabled }, object: nil)\n+                let waitResult = XCTWaiter.wait(for: [changed], timeout: 5)\n+                // Read the current value before diagnostics; the wait can expire while Settings rebuilds its controls.\n+                let actual = nativeSwitchValue(settings.switches[label])\n+                let observationSeconds = ProcessInfo.processInfo.systemUptime - observationStart\n+                emitJSON(marker: \"ACE_NATIVE_SWITCH_RESULT\", payload: [\n+                    \"actual\": actual.map { $0 as Any } ?? NSNull(), \"attempt\": taps, \"expected\": enabled,\n+                    \"label\": label, \"seconds\": observationSeconds, \"wait\": waitResult.rawValue\n+                ])\n+                if actual == enabled { return true }\n+                if taps == 1 {\n+                    firstAttemptActual = actual\n+                    addScreenshot(of: settings, named: \"Accessibility settings \\(label) first attempt did not change\")\n+                    continue\n+                }\n+                addScreenshot(of: settings, named: \"Accessibility settings \\(label) did not change after two attempts\")\n+                XCTFail(\"Settings control \\(label): expected \\(enabled), actual \\(String(describing: actual)), attempts \\(taps), first attempt actual \\(String(describing: firstAttemptActual))\")\n+                return false\n+            }\n         }\n-        return true\n     }\n \n     private func returnToSettingsRoot(_ settings: XCUIApplication) -> Bool {\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/ace-private/mcx19b-orientation-repair-20260915/negative-probe-worktree",
      "HEAD": "464671562cfb0249e859ffc8905a8f8c5ef17e55",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\nindex b5ab0b0..0e56a65 100644\n--- a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n+++ b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n@@ -70,7 +70,7 @@ final class ACEClientAppUITests: XCTestCase {\n         if request.mode == \"set\" {\n             guard let boldText = request.boldText, let reduceMotion = request.reduceMotion,\n                   let increaseContrast = request.increaseContrast, let orientation = request.orientation,\n-                  assertRequestedOrientation(in: app, requested: orientation),\n+                  assertRequestedOrientation(in: app, requested: orientation == \"portrait\" ? \"landscape\" : \"portrait\"),\n                   requireCoverage(\n                     observed.orientation == orientation && observed.boldText == boldText && observed.reduceMotion == reduceMotion\n                         && observed.increaseContrast == increaseContrast,\n@@ -163,12 +163,38 @@ final class ACEClientAppUITests: XCTestCase {\n     }\n \n     private func assertRequestedOrientation(in app: XCUIApplication, requested: String) -> Bool {\n-        let expectation = XCTNSPredicateExpectation(predicate: NSPredicate { _, _ in\n-            let frame = app.frame\n-            return requested == \"landscape\" ? frame.width > frame.height : frame.height > frame.width\n-        }, object: nil)\n-        return requireCoverage(XCTWaiter.wait(for: [expectation], timeout: 5) == .completed,\n-                               \"App frame did not reach requested \\(requested) orientation\")\n+        emitJSON(marker: \"ACE_ORIENTATION_READ_STARTED\", payload: [\"requested\": requested])\n+        let readStart = ProcessInfo.processInfo.systemUptime\n+        let frame = app.frame\n+        let elapsedReadSeconds = ProcessInfo.processInfo.systemUptime - readStart\n+        let hasUsableDimensions = frame.width.isFinite && frame.height.isFinite\n+            && frame.width > 0 && frame.height > 0\n+        let matches = hasUsableDimensions && (requested == \"landscape\"\n+            ? frame.width > frame.height\n+            : frame.height > frame.width)\n+        let outcome = hasUsableDimensions ? (matches ? \"match\" : \"mismatch\") : \"readFailure\"\n+\n+        emitJSON(marker: \"ACE_ORIENTATION_FRAME\", payload: [\n+            \"requested\": requested,\n+            \"outcome\": outcome,\n+            \"elapsedReadSeconds\": elapsedReadSeconds,\n+            \"frame\": [\n+                \"x\": \"\\(frame.origin.x)\",\n+                \"y\": \"\\(frame.origin.y)\",\n+                \"width\": \"\\(frame.width)\",\n+                \"height\": \"\\(frame.height)\"\n+            ]\n+        ])\n+\n+        guard requireCoverage(\n+            hasUsableDimensions,\n+            \"Could not read a usable native app frame for requested \\(requested) orientation\"\n+        ) else { return false }\n+\n+        return requireCoverage(\n+            matches,\n+            \"Native app frame does not match requested \\(requested) orientation\"\n+        )\n     }\n \n     private func waitForCoverageState(in app: XCUIApplication, scenario: String) -> Bool {\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/ace-private/mcx19b-orientation-repair-20260915/release-margin-impl",
      "HEAD": "eef4b163726444eb3c7bfd6e936cf9c8b53e46bc",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientApp/DebugScenario.swift b/ios/ACEClientApp/ACEClientApp/DebugScenario.swift\nindex db9fc6d..c93b918 100644\n--- a/ios/ACEClientApp/ACEClientApp/DebugScenario.swift\n+++ b/ios/ACEClientApp/ACEClientApp/DebugScenario.swift\n@@ -10,6 +10,15 @@ enum UITestScenario: String {\n         guard let raw = ProcessInfo.processInfo.environment[\"ACE_UI_TEST_SCENARIO\"] else { return nil }\n         return UITestScenario(rawValue: raw)\n     }\n+\n+    var usesReleaseScroll: Bool {\n+        switch self {\n+        case .release, .noConclusion, .noActions, .copyConfirmation, .multiAction:\n+            true\n+        default:\n+            false\n+        }\n+    }\n }\n \n struct ScenarioRootView: View {\ndiff --git a/ios/ACEClientApp/ACEClientApp/Views.swift b/ios/ACEClientApp/ACEClientApp/Views.swift\nindex d613e14..78943d1 100644\n--- a/ios/ACEClientApp/ACEClientApp/Views.swift\n+++ b/ios/ACEClientApp/ACEClientApp/Views.swift\n@@ -8,7 +8,11 @@ struct RootView: View {\n     var body: some View {\n         #if DEBUG\n         if let scenario = UITestScenario.current {\n-            ScenarioRootView(scenario: scenario).padding()\n+            if scenario.usesReleaseScroll {\n+                ScenarioRootView(scenario: scenario)\n+            } else {\n+                ScenarioRootView(scenario: scenario).padding()\n+            }\n         }\n         else { content }\n         #else\n@@ -37,9 +41,14 @@ struct RootView: View {\n                 SafeMessageView(message: message, action: (deletionOnlyRecovery ? \"Reset saved sign-in\" : \"Try again\", deletionOnlyRecovery ? state.resetSavedSignIn : { state.retry(.deletion) }))\n             }\n         }\n-        .padding()\n+        .padding(isReleasePresentation(presentation) ? 0 : nil)\n         .modifier(ErrorAnnouncement(message: presentation.errorMessage, event: state.errorAnnouncementEvent))\n     }\n+\n+    private func isReleasePresentation(_ presentation: ScreenState.Presentation) -> Bool {\n+        if case .release = presentation { return true }\n+        return false\n+    }\n }\n \n private struct ErrorAnnouncement: ViewModifier {\n@@ -202,6 +211,7 @@ struct ReleaseView: View {\n                 .accessibilityLabel(\"Sign out\")\n             }\n             .frame(maxWidth: .infinity, alignment: .leading)\n+            .padding()\n         }\n         .tint(.primary)\n     }\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/ace-private/mcx19b-orientation-repair-20260915/runner-serial-worktree",
      "HEAD": "a84f67010362c9e87e49576af8cf40b2c23a807b",
      "detached": true,
      "diff": "diff --git a/tools/ace_ios_local.py b/tools/ace_ios_local.py\nindex 57c74dd..b5b63e0 100644\n--- a/tools/ace_ios_local.py\n+++ b/tools/ace_ios_local.py\n@@ -403,7 +403,7 @@ def restore_boot_state(identifier: str, initial_state: str) -> None:\n         checked([\"xcrun\", \"simctl\", \"shutdown\", identifier], timeout=120)\n \n \n-def restore_simctl_settings(identifier: str, previous: dict[str, str], evidence: Path, device: str) -> list[str]:\n+def restore_simctl_settings(identifier: str, previous: dict[str, str], evidence: Path, device: str, phase: str) -> list[str]:\n     failures: list[str] = []\n     records: list[dict[str, object]] = []\n     for setting, value in previous.items():\n@@ -411,8 +411,33 @@ def restore_simctl_settings(identifier: str, previous: dict[str, str], evidence:\n         records.append(result)\n         if result.get(\"exit\") != 0:\n             failures.append(setting)\n-    (evidence / \"restoration\" / f\"{device}-simctl-commands.json\").parent.mkdir(parents=True, exist_ok=True)\n-    (evidence / \"restoration\" / f\"{device}-simctl-commands.json\").write_text(json.dumps(records, indent=2, sort_keys=True), encoding=\"utf-8\")\n+    path = evidence / \"restoration\" / f\"{device}-simctl-{phase}-commands.json\"\n+    path.parent.mkdir(parents=True, exist_ok=True)\n+    path.write_text(json.dumps(records, indent=2, sort_keys=True), encoding=\"utf-8\")\n+    return failures\n+\n+\n+def restore_device_configuration(identifier: str, previous: dict[str, str] | None,\n+                                 previous_native_flags: dict[str, object] | None, template: Path,\n+                                 evidence: Path, device: str, phase: str) -> list[str]:\n+    failures: list[str] = []\n+    if previous is not None:\n+        failures.extend(f\"{device}:{field}\" for field in restore_simctl_settings(identifier, previous, evidence, device, phase))\n+        try:\n+            observed_simctl = current_simctl_settings(identifier)\n+            (evidence / \"restoration\" / f\"{device}-simctl-{phase}-readback.json\").write_text(\n+                json.dumps({\"expected\": previous, \"observed\": observed_simctl}, indent=2, sort_keys=True), encoding=\"utf-8\")\n+            if observed_simctl != previous:\n+                failures.append(f\"{device}:simctl-readback\")\n+        except RunnerError:\n+            failures.append(f\"{device}:simctl-readback\")\n+    if previous_native_flags is not None:\n+        try:\n+            restored = native_flags(identifier, template, evidence, f\"{device}-restore-{phase}\", {\"mode\": \"set\", **previous_native_flags})\n+            if restored != previous_native_flags:\n+                failures.append(f\"{device}:native-flags\")\n+        except RunnerError:\n+            failures.append(f\"{device}:native-restore\")\n     return failures\n \n \n@@ -803,38 +828,69 @@ def run_coverage(cases: tuple[Case, ...], evidence: Path, retained_pilot: dict[s\n     assert isinstance(devices, dict)\n     reused = set() if retained_pilot is None else eligible_pilot_cases(retained_pilot)\n     execution_cases = tuple(case for case in cases if canonical_case(case.device, case.scenario, case.settings.payload()) not in reused)\n+    unordered_batch_plan = coverage_batches(execution_cases)\n+    batch_plan = tuple(batch for device in DEVICES for batch in unordered_batch_plan if batch[0] == device)\n+    final_batch_index = {device: index for index, (device, _settings, _cases) in enumerate(batch_plan, 1)}\n     previous: dict[str, dict[str, str]] = {}\n     previous_native_flags: dict[str, dict[str, object]] = {}\n     initial_boot_states: dict[str, str] = {}\n-    booted_by_runner: set[str] = set()\n+    prepared_devices: set[str] = set()\n+    batch_restored_devices: set[str] = set()\n+    post_boot_restored_devices: set[str] = set()\n+    failed_restoration_devices: set[str] = set()\n     restoration_failures: list[str] = []\n     records: list[dict[str, object]] = []\n     primary_error: RunnerError | None = None\n     try:\n-        for device, data in devices.items():\n+        for device in DEVICES:\n+            if device not in devices:\n+                continue\n+            data = devices[device]\n+            assert isinstance(data, dict)\n+            initial_boot_states[device] = simulator_state(str(data[\"udid\"]))\n+        for device in DEVICES:\n+            if device not in devices or initial_boot_states[device] != \"Booted\":\n+                continue\n+            data = devices[device]\n             assert isinstance(data, dict)\n             identifier = str(data[\"udid\"])\n-            initial_boot_states[device] = simulator_state(identifier)\n-            if initial_boot_states[device] == \"Shutdown\":\n-                # Restore even if boot succeeds but bootstatus later fails.\n-                booted_by_runner.add(device)\n-            boot_required_simulator(identifier, initial_boot_states[device])\n             previous[device] = current_simctl_settings(identifier)\n             previous_native_flags[device] = native_flags(identifier, Path(str(build_record[\"template\"])), evidence, f\"{device}-read\", {\"mode\": \"read\"})\n-        for index, (device, settings, batch_cases) in enumerate(coverage_batches(execution_cases), 1):\n+            checked([\"xcrun\", \"simctl\", \"shutdown\", identifier], timeout=120)\n+        for index, (device, settings, batch_cases) in enumerate(batch_plan, 1):\n             if candidate() != candidate_record:\n                 raise RunnerError(\"candidate identity changed before native batch\")\n             data = devices[device]\n             assert isinstance(data, dict)\n-            native_flags(str(data[\"udid\"]), Path(str(build_record[\"template\"])), evidence, f\"{index:03d}-set\", {\n+            identifier = str(data[\"udid\"])\n+            if device not in prepared_devices:\n+                boot_required_simulator(identifier, simulator_state(identifier))\n+                if device not in previous:\n+                    previous[device] = current_simctl_settings(identifier)\n+                    previous_native_flags[device] = native_flags(identifier, Path(str(build_record[\"template\"])), evidence, f\"{device}-read\", {\"mode\": \"read\"})\n+                prepared_devices.add(device)\n+            native_flags(identifier, Path(str(build_record[\"template\"])), evidence, f\"{index:03d}-set\", {\n                 \"mode\": \"set\", \"boldText\": settings.boldText, \"reduceMotion\": settings.reduceMotion,\n                 \"increaseContrast\": settings.increaseContrast, \"orientation\": settings.orientation,\n             })\n-            records.append(run_batch(device, str(data[\"udid\"]), settings, batch_cases, build_record, evidence, index))\n+            records.append(run_batch(device, identifier, settings, batch_cases, build_record, evidence, index))\n             (evidence / \"batches\" / f\"{index:03d}\" / \"checkpoint.json\").write_text(\n                 json.dumps({\"status\": \"passed\", \"record\": records[-1]}, indent=2, sort_keys=True), encoding=\"utf-8\")\n             if candidate() != candidate_record:\n                 raise RunnerError(\"candidate identity changed after native batch\")\n+            if index == final_batch_index[device]:\n+                device_restore_failures = restore_device_configuration(\n+                    identifier, previous[device], previous_native_flags[device], Path(str(build_record[\"template\"])), evidence, device, \"batch\"\n+                )\n+                batch_restored_devices.add(device)\n+                restoration_failures.extend(device_restore_failures)\n+                if device_restore_failures:\n+                    failed_restoration_devices.add(device)\n+                    raise RunnerError(\"simulator setting restoration failed: \" + \", \".join(restoration_failures))\n+                if index == len(batch_plan):\n+                    post_boot_restored_devices.add(device)\n+                if initial_boot_states[device] == \"Shutdown\" or index != len(batch_plan):\n+                    checked([\"xcrun\", \"simctl\", \"shutdown\", identifier], timeout=120)\n     except RunnerError as error:\n         primary_error = error\n         (evidence / \"coverage-checkpoint.json\").write_text(json.dumps({\"status\": \"failed\", \"candidate\": candidate_record,\n@@ -844,29 +900,33 @@ def run_coverage(cases: tuple[Case, ...], evidence: Path, retained_pilot: dict[s\n             data = devices[device]\n             assert isinstance(data, dict)\n             identifier = str(data[\"udid\"])\n-            before = previous.get(device)\n-            if before is not None:\n-                restoration_failures.extend(f\"{device}:{field}\" for field in restore_simctl_settings(identifier, before, evidence, device))\n-                try:\n-                    observed_simctl = current_simctl_settings(identifier)\n-                    (evidence / \"restoration\" / f\"{device}-simctl-readback.json\").write_text(\n-                        json.dumps({\"expected\": before, \"observed\": observed_simctl}, indent=2, sort_keys=True), encoding=\"utf-8\")\n-                    if observed_simctl != before:\n-                        restoration_failures.append(f\"{device}:simctl-readback\")\n-                except RunnerError:\n-                    restoration_failures.append(f\"{device}:simctl-readback\")\n-            if device in previous_native_flags:\n+            if initial_boot_states[device] == \"Shutdown\":\n+                if device in prepared_devices and device not in batch_restored_devices and device not in failed_restoration_devices:\n+                    device_restore_failures = restore_device_configuration(\n+                        identifier, previous.get(device), previous_native_flags.get(device), Path(str(build_record[\"template\"])), evidence, device, \"failure\"\n+                    )\n+                    batch_restored_devices.add(device)\n+                    restoration_failures.extend(device_restore_failures)\n+                    if device_restore_failures:\n+                        failed_restoration_devices.add(device)\n                 try:\n-                    restored = native_flags(identifier, Path(str(build_record[\"template\"])), evidence, f\"{device}-restore\", {\"mode\": \"set\", **previous_native_flags[device]})\n-                    if restored != previous_native_flags[device]:\n-                        restoration_failures.append(f\"{device}:native-flags\")\n+                    restore_boot_state(identifier, initial_boot_states[device])\n                 except RunnerError:\n-                    restoration_failures.append(f\"{device}:native-restore\")\n-            if device in booted_by_runner:\n+                    restoration_failures.append(f\"{device}:boot-state\")\n+            else:\n                 try:\n-                    restore_boot_state(identifier, initial_boot_states[device])\n+                    boot_required_simulator(identifier, simulator_state(identifier))\n                 except RunnerError:\n                     restoration_failures.append(f\"{device}:boot-state\")\n+                    continue\n+                if device not in post_boot_restored_devices and device in previous and device not in failed_restoration_devices:\n+                    device_restore_failures = restore_device_configuration(\n+                        identifier, previous[device], previous_native_flags.get(device), Path(str(build_record[\"template\"])), evidence, device, \"post-boot\"\n+                    )\n+                    post_boot_restored_devices.add(device)\n+                    restoration_failures.extend(device_restore_failures)\n+                    if device_restore_failures:\n+                        failed_restoration_devices.add(device)\n     if primary_error is not None:\n         suffix = \"\" if not restoration_failures else \"; restoration failures: \" + \", \".join(restoration_failures)\n         (evidence / \"coverage-final-failure.json\").write_text(json.dumps({\"candidate\": candidate_record,\ndiff --git a/tools/tests/test_ace_ios_local.py b/tools/tests/test_ace_ios_local.py\nindex 7e7c318..3ea7f1b 100644\n--- a/tools/tests/test_ace_ios_local.py\n+++ b/tools/tests/test_ace_ios_local.py\n@@ -33,6 +33,7 @@ class CoverageMatrixTests(unittest.TestCase):\n         cases = ace.pilot_cases()\n         self.assertEqual(len(cases), 22)\n         self.assertEqual(sum(case.audit for case in cases), 20)\n+        self.assertEqual(sum(ace.expected_audit_invocations(case) for case in cases), 36)\n         self.assertEqual([case.scenario for case in cases if not case.audit], [\"release\", \"release\"])\n         configuration = lambda case: (case.device, case.scenario, case.settings)\n         self.assertEqual(len({configuration(case) for case in cases}), 22)\n@@ -335,6 +336,167 @@ class PartialBootRecoveryTests(unittest.TestCase):\n             self.assertEqual(failure[\"restorationFailures\"], [])\n \n \n+class SerialDeviceExecutionTests(unittest.TestCase):\n+    def run_pilot(self, initial_states, failure_device=None, initial_orientations=None, cases=None):\n+        identity = {\"commit\": \"controlled\", \"status\": \"clean\"}\n+        devices = {device: {\"udid\": device} for device in ace.DEVICES}\n+        states = dict(initial_states)\n+        events = []\n+        settings = {\"appearance\": \"light\", \"content_size\": \"large\", \"increase_contrast\": \"disabled\"}\n+        flags = {\"boldText\": False, \"reduceMotion\": False, \"increaseContrast\": False, \"orientation\": \"portrait\"}\n+\n+        initial_orientations = initial_orientations or {}\n+        cases = ace.pilot_cases() if cases is None else cases\n+        initial_flags = {device: {**flags, \"orientation\": initial_orientations.get(device, \"portrait\")}\n+                         for device in ace.DEVICES}\n+        native_values = {device: dict(value) for device, value in initial_flags.items()}\n+\n+        def simulator_state(identifier):\n+            events.append((\"state\", identifier))\n+            return states[identifier]\n+\n+        def boot(identifier, initial_state):\n+            events.append((\"boot\", identifier))\n+            if states[identifier] == \"Shutdown\":\n+                native_values[identifier][\"orientation\"] = \"portrait\"\n+            states[identifier] = \"Booted\"\n+            return initial_state == \"Shutdown\"\n+\n+        def checked(command, **_kwargs):\n+            if command[:3] == [\"xcrun\", \"simctl\", \"shutdown\"]:\n+                events.append((\"shutdown\", command[3]))\n+                states[command[3]] = \"Shutdown\"\n+            return {\"exit\": 0}\n+\n+        def native_flags(identifier, _template, _evidence, label, request):\n+            events.append((\"nativeFlags\", identifier, label, request[\"mode\"]))\n+            if request[\"mode\"] == \"read\":\n+                return dict(native_values[identifier])\n+            native_values[identifier] = {key: request[key] for key in flags}\n+            return dict(native_values[identifier])\n+\n+        def run_batch(device, _identifier, _settings, _cases, _build, _evidence, index):\n+            events.append((\"batch\", device, index))\n+            (evidence / \"batches\" / f\"{index:03d}\").mkdir(parents=True, exist_ok=True)\n+            if device == failure_device:\n+                raise ace.RunnerError(\"first device native failure\")\n+            return {\"device\": device, \"nativeTestCount\": 1, \"caseRecords\": []}\n+\n+        with tempfile.TemporaryDirectory() as directory:\n+            evidence = Path(directory)\n+            (evidence / \"restoration\").mkdir()\n+            with patch.object(ace, \"preflight\", return_value={\"devices\": devices}), \\\n+                 patch.object(ace, \"candidate\", return_value=identity), \\\n+                 patch.object(ace, \"build\", return_value={\"template\": \"unused\"}), \\\n+                 patch.object(ace, \"simulator_state\", side_effect=simulator_state), \\\n+                 patch.object(ace, \"boot_required_simulator\", side_effect=boot), \\\n+                 patch.object(ace, \"checked\", side_effect=checked), \\\n+                 patch.object(ace, \"current_simctl_settings\", return_value=settings), \\\n+                 patch.object(ace, \"native_flags\", side_effect=native_flags), \\\n+                 patch.object(ace, \"restore_simctl_settings\", side_effect=lambda identifier, _previous, _evidence, _device, phase: events.append((\"restoreSettings\", identifier, phase)) or []), \\\n+                 patch.object(ace, \"run_batch\", side_effect=run_batch):\n+                if failure_device is None:\n+                    result = ace.run_coverage(cases, evidence)\n+                else:\n+                    with self.assertRaisesRegex(ace.RunnerError, \"first device native failure\"):\n+                        ace.run_coverage(cases, evidence)\n+                    result = None\n+        return events, states, result, native_values\n+\n+    def test_runs_one_required_device_before_booting_the_next(self):\n+        events, states, result, _native_values = self.run_pilot({device: \"Shutdown\" for device in ace.DEVICES})\n+        first_iphone17_batch = next(index for index, event in enumerate(events) if event[0] == \"batch\" and event[1] == \"iPhone 17\")\n+        pro_max_boot = next(index for index, event in enumerate(events) if event == (\"boot\", \"iPhone 17 Pro Max\"))\n+        iphone17_shutdown = next(index for index, event in enumerate(events) if event == (\"shutdown\", \"iPhone 17\"))\n+        self.assertLess(first_iphone17_batch, pro_max_boot)\n+        self.assertLess(iphone17_shutdown, pro_max_boot)\n+        self.assertEqual(states, {device: \"Shutdown\" for device in ace.DEVICES})\n+        self.assertEqual(result[\"caseCount\"], 22)\n+        self.assertEqual(sum(ace.expected_audit_invocations(case) for case in ace.pilot_cases()), 36)\n+\n+    def test_full_plan_finishes_one_device_before_starting_the_next(self):\n+        events, states, result, _native_values = self.run_pilot(\n+            {device: \"Shutdown\" for device in ace.DEVICES}, cases=ace.coverage_cases()\n+        )\n+        batch_devices = [event[1] for event in events if event[0] == \"batch\"]\n+        self.assertEqual(batch_devices, sorted(batch_devices, key=ace.DEVICES.index))\n+        pro_max_boot = next(index for index, event in enumerate(events) if event == (\"boot\", \"iPhone 17 Pro Max\"))\n+        last_iphone17_batch = max(index for index, event in enumerate(events) if event[0] == \"batch\" and event[1] == \"iPhone 17\")\n+        self.assertLess(last_iphone17_batch, pro_max_boot)\n+        self.assertEqual(states, {device: \"Shutdown\" for device in ace.DEVICES})\n+        self.assertEqual(result[\"caseCount\"], len(ace.coverage_cases()))\n+\n+    def test_parks_initially_booted_idle_device_and_restores_boot_states(self):\n+        events, states, _result, native_values = self.run_pilot({device: \"Booted\" for device in ace.DEVICES},\n+                                                                  initial_orientations={device: \"landscape\" for device in ace.DEVICES})\n+        first_iphone17_batch = next(index for index, event in enumerate(events) if event[0] == \"batch\" and event[1] == \"iPhone 17\")\n+        pro_max_shutdown = next(index for index, event in enumerate(events) if event == (\"shutdown\", \"iPhone 17 Pro Max\"))\n+        self.assertLess(pro_max_shutdown, first_iphone17_batch)\n+        pro_max_native_read = next(index for index, event in enumerate(events) if event == (\"nativeFlags\", \"iPhone 17 Pro Max\", \"iPhone 17 Pro Max-read\", \"read\"))\n+        self.assertLess(pro_max_native_read, pro_max_shutdown)\n+        self.assertEqual(states, {device: \"Booted\" for device in ace.DEVICES})\n+        expected_flags = {\"boldText\": False, \"reduceMotion\": False, \"increaseContrast\": False, \"orientation\": \"landscape\"}\n+        self.assertEqual(native_values, {device: dict(expected_flags) for device in ace.DEVICES})\n+\n+    def test_uses_unique_native_restore_evidence_labels(self):\n+        events, _states, _result, _native_values = self.run_pilot(\n+            {device: \"Booted\" for device in ace.DEVICES}, initial_orientations={device: \"landscape\" for device in ace.DEVICES}\n+        )\n+        restore_labels = [event[2] for event in events if event[0] == \"nativeFlags\" and \"-restore-\" in event[2]]\n+        self.assertEqual(len(restore_labels), len(set(restore_labels)))\n+        self.assertIn(\"iPhone 17-restore-batch\", restore_labels)\n+        self.assertIn(\"iPhone 17-restore-post-boot\", restore_labels)\n+        self.assertIn(\"iPhone 17 Pro Max-restore-batch\", restore_labels)\n+        simctl_labels = [(event[1], event[2]) for event in events if event[0] == \"restoreSettings\"]\n+        self.assertEqual(len(simctl_labels), len(set(simctl_labels)))\n+        self.assertIn((\"iPhone 17\", \"batch\"), simctl_labels)\n+        self.assertIn((\"iPhone 17\", \"post-boot\"), simctl_labels)\n+\n+    def test_simctl_restoration_evidence_includes_phase(self):\n+        previous = {\"appearance\": \"light\", \"content_size\": \"large\", \"increase_contrast\": \"disabled\"}\n+        with tempfile.TemporaryDirectory() as directory:\n+            evidence = Path(directory)\n+            with patch.object(ace, \"command\", return_value={\"exit\": 0}), \\\n+                 patch.object(ace, \"current_simctl_settings\", return_value=previous):\n+                self.assertEqual(ace.restore_device_configuration(\"test-udid\", previous, None, Path(\"unused\"), evidence, \"iPhone 17\", \"batch\"), [])\n+                self.assertEqual(ace.restore_device_configuration(\"test-udid\", previous, None, Path(\"unused\"), evidence, \"iPhone 17\", \"post-boot\"), [])\n+            names = {path.name for path in (evidence / \"restoration\").iterdir()}\n+        self.assertEqual(names, {\n+            \"iPhone 17-simctl-batch-commands.json\", \"iPhone 17-simctl-batch-readback.json\",\n+            \"iPhone 17-simctl-post-boot-commands.json\", \"iPhone 17-simctl-post-boot-readback.json\",\n+        })\n+\n+    def test_records_all_initial_boot_states_before_parking(self):\n+        events, _states, _result, _native_values = self.run_pilot({device: \"Booted\" for device in ace.DEVICES})\n+        first_shutdown = next(index for index, event in enumerate(events) if event[0] == \"shutdown\")\n+        initial_state_reads = [index for index, event in enumerate(events) if event[0] == \"state\"][:len(ace.DEVICES)]\n+        self.assertEqual(len(initial_state_reads), len(ace.DEVICES))\n+        self.assertTrue(all(index < first_shutdown for index in initial_state_reads))\n+\n+    def test_first_device_failure_does_not_start_the_second_device(self):\n+        events, states, result, _native_values = self.run_pilot({device: \"Shutdown\" for device in ace.DEVICES}, failure_device=\"iPhone 17\")\n+        self.assertIsNone(result)\n+        self.assertFalse(any(event[0] in {\"boot\", \"nativeFlags\", \"batch\"} and event[1] == \"iPhone 17 Pro Max\" for event in events))\n+        self.assertEqual(states, {device: \"Shutdown\" for device in ace.DEVICES})\n+\n+    def test_mixed_boot_state_restores_landscape_after_final_boot(self):\n+        initial_states = {\"iPhone 17\": \"Shutdown\", \"iPhone 17 Pro Max\": \"Booted\"}\n+        _events, states, _result, native_values = self.run_pilot(\n+            initial_states, initial_orientations={\"iPhone 17 Pro Max\": \"landscape\"}\n+        )\n+        self.assertEqual(states, initial_states)\n+        self.assertEqual(native_values[\"iPhone 17 Pro Max\"][\"orientation\"], \"landscape\")\n+\n+    def test_first_device_failure_restores_parked_landscape_device_after_final_boot(self):\n+        initial_states = {\"iPhone 17\": \"Shutdown\", \"iPhone 17 Pro Max\": \"Booted\"}\n+        events, states, result, native_values = self.run_pilot(\n+            initial_states, failure_device=\"iPhone 17\", initial_orientations={\"iPhone 17 Pro Max\": \"landscape\"}\n+        )\n+        self.assertIsNone(result)\n+        self.assertFalse(any(event[0] == \"batch\" and event[1] == \"iPhone 17 Pro Max\" for event in events))\n+        self.assertEqual(states, initial_states)\n+        self.assertEqual(native_values[\"iPhone 17 Pro Max\"][\"orientation\"], \"landscape\")\n+\n class CommandAndResourceEvidenceTests(unittest.TestCase):\n     def full_gate_fixture(self, root: Path):\n         candidate = {\"commit\": \"candidate\", \"status\": \"clean\"}\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/ace-private/mcx19b-orientation-repair-20260915/serial-negative-probe-worktree",
      "HEAD": "905004fc71107565abf2d68cb05fc581cf5bf310",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\nindex b11bc9c..666d286 100644\n--- a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n+++ b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n@@ -115,7 +115,7 @@ final class ACEClientAppUITests: XCTestCase {\n         let app = launchForCoverageObservation(scenario: coverageCase.scenario)\n         defer { app.terminate() }\n         guard waitForCoverageState(in: app, scenario: coverageCase.scenario),\n-              assertRequestedOrientation(in: app, requested: coverageCase.settings.orientation),\n+              assertRequestedOrientation(in: app, requested: coverageCase.settings.orientation == \"portrait\" ? \"landscape\" : \"portrait\"),\n               let observed = observedSettings(in: app),\n               requireCoverage(observed == coverageCase.settings,\n                               \"Observed settings differ from requested settings for \\(coverageCase.id): \\(observed.payload)\") else { return false }\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/ace-private/mcx19b-orientation-repair-20260915/shutdown-guard-worktree-v3",
      "HEAD": "905004fc71107565abf2d68cb05fc581cf5bf310",
      "detached": true,
      "diff": "diff --git a/tools/ace_ios_local.py b/tools/ace_ios_local.py\nindex b5b63e0..17c4683 100644\n--- a/tools/ace_ios_local.py\n+++ b/tools/ace_ios_local.py\n@@ -400,7 +400,8 @@ def boot_required_simulator(identifier: str, initial_state: str) -> bool:\n \n def restore_boot_state(identifier: str, initial_state: str) -> None:\n     if initial_state == \"Shutdown\":\n-        checked([\"xcrun\", \"simctl\", \"shutdown\", identifier], timeout=120)\n+        if simulator_state(identifier) != \"Shutdown\":\n+            checked([\"xcrun\", \"simctl\", \"shutdown\", identifier], timeout=120)\n \n \n def restore_simctl_settings(identifier: str, previous: dict[str, str], evidence: Path, device: str, phase: str) -> list[str]:\ndiff --git a/tools/tests/test_ace_ios_local.py b/tools/tests/test_ace_ios_local.py\nindex 3ea7f1b..4705ce0 100644\n--- a/tools/tests/test_ace_ios_local.py\n+++ b/tools/tests/test_ace_ios_local.py\n@@ -147,15 +147,37 @@ class NativeRecordTests(unittest.TestCase):\n                 ace.selected_test_environment(path, \"ACEClientAppUITests\")\n \n     def test_selected_boot_helpers_restore_only_an_initially_shutdown_simulator(self):\n-        original = ace.checked\n         commands = []\n-        ace.checked = lambda command, **_kwargs: commands.append(command) or {\"exit\": 0}\n-        try:\n+        def checked(command, **_kwargs):\n+            commands.append(command)\n+            return {\"exit\": 0}\n+\n+        with patch.object(ace, \"simulator_state\", return_value=\"Booted\"), \\\n+             patch.object(ace, \"checked\", side_effect=checked):\n             self.assertTrue(ace.boot_required_simulator(\"id\", \"Shutdown\"))\n             ace.restore_boot_state(\"id\", \"Shutdown\")\n-            self.assertEqual(commands, [[\"xcrun\", \"simctl\", \"boot\", \"id\"], [\"xcrun\", \"simctl\", \"bootstatus\", \"id\", \"-b\"], [\"xcrun\", \"simctl\", \"shutdown\", \"id\"]])\n-        finally:\n-            ace.checked = original\n+        self.assertEqual(commands, [[\"xcrun\", \"simctl\", \"boot\", \"id\"], [\"xcrun\", \"simctl\", \"bootstatus\", \"id\", \"-b\"], [\"xcrun\", \"simctl\", \"shutdown\", \"id\"]])\n+\n+    def test_restore_boot_state_skips_shutdown_when_the_simulator_is_already_shutdown(self):\n+        with patch.object(ace, \"simulator_state\", return_value=\"Shutdown\") as simulator_state, \\\n+             patch.object(ace, \"checked\") as checked:\n+            ace.restore_boot_state(\"id\", \"Shutdown\")\n+        simulator_state.assert_called_once_with(\"id\")\n+        checked.assert_not_called()\n+\n+    def test_restore_boot_state_propagates_state_and_shutdown_errors(self):\n+        with self.subTest(\"unavailable-state\"):\n+            with patch.object(ace, \"simulator_state\", side_effect=ace.RunnerError(\"required simulator state is unavailable\")), \\\n+                 patch.object(ace, \"checked\") as checked:\n+                with self.assertRaisesRegex(ace.RunnerError, \"state is unavailable\"):\n+                    ace.restore_boot_state(\"id\", \"Shutdown\")\n+            checked.assert_not_called()\n+\n+        with self.subTest(\"shutdown-error\"):\n+            with patch.object(ace, \"simulator_state\", return_value=\"Booted\"), \\\n+                 patch.object(ace, \"checked\", side_effect=ace.RunnerError(\"command failed: xcrun simctl shutdown id\")):\n+                with self.assertRaisesRegex(ace.RunnerError, \"simctl shutdown\"):\n+                    ace.restore_boot_state(\"id\", \"Shutdown\")\n \n     def test_pilot_readiness_rejects_loose_prose_and_bad_artifact_hash(self):\n         candidate = {\"commit\": \"abc\", \"status\": \"clean\", \"sourceHashes\": {}, \"statusDetail\": \"\", \"workingTreeSha256\": \"x\"}\n@@ -364,6 +386,8 @@ class SerialDeviceExecutionTests(unittest.TestCase):\n \n         def checked(command, **_kwargs):\n             if command[:3] == [\"xcrun\", \"simctl\", \"shutdown\"]:\n+                if states[command[3]] == \"Shutdown\":\n+                    raise ace.RunnerError(\"duplicate simulator shutdown\")\n                 events.append((\"shutdown\", command[3]))\n                 states[command[3]] = \"Shutdown\"\n             return {\"exit\": 0}\n@@ -411,6 +435,8 @@ class SerialDeviceExecutionTests(unittest.TestCase):\n         self.assertLess(first_iphone17_batch, pro_max_boot)\n         self.assertLess(iphone17_shutdown, pro_max_boot)\n         self.assertEqual(states, {device: \"Shutdown\" for device in ace.DEVICES})\n+        shutdowns = {device: sum(event == (\"shutdown\", device) for event in events) for device in ace.DEVICES}\n+        self.assertEqual(shutdowns, {device: 1 for device in ace.DEVICES})\n         self.assertEqual(result[\"caseCount\"], 22)\n         self.assertEqual(sum(ace.expected_audit_invocations(case) for case in ace.pilot_cases()), 36)\n \n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/ace-private/mcx19b-orientation-repair-20260915/shutdown-negative-probe-worktree",
      "HEAD": "eef4b163726444eb3c7bfd6e936cf9c8b53e46bc",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\nindex b11bc9c..666d286 100644\n--- a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n+++ b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n@@ -115,7 +115,7 @@ final class ACEClientAppUITests: XCTestCase {\n         let app = launchForCoverageObservation(scenario: coverageCase.scenario)\n         defer { app.terminate() }\n         guard waitForCoverageState(in: app, scenario: coverageCase.scenario),\n-              assertRequestedOrientation(in: app, requested: coverageCase.settings.orientation),\n+              assertRequestedOrientation(in: app, requested: coverageCase.settings.orientation == \"portrait\" ? \"landscape\" : \"portrait\"),\n               let observed = observedSettings(in: app),\n               requireCoverage(observed == coverageCase.settings,\n                               \"Observed settings differ from requested settings for \\(coverageCase.id): \\(observed.payload)\") else { return false }\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/Developer/sqe-platform-audit-lifecycle",
      "HEAD": "1b38b90e9a949b7e73965602612c11be6f19e9fc",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientApp/ACEClientAppApp.swift b/ios/ACEClientApp/ACEClientApp/ACEClientAppApp.swift\nindex f3c6b6b..6f08190 100644\n--- a/ios/ACEClientApp/ACEClientApp/ACEClientAppApp.swift\n+++ b/ios/ACEClientApp/ACEClientApp/ACEClientAppApp.swift\n@@ -1,6 +1,7 @@\n import Foundation\n import SwiftUI\n import UIKit\n+import OSLog\n \n @main\n struct ACEClientApp: App {\n@@ -105,6 +106,10 @@ private struct EffectiveInterfaceStyleIndicator: View {\n             .background(.background)\n             .accessibilityIdentifier(\"Effective interface style\")\n             .accessibilityValue(accessibilityValue)\n+            .onAppear { AuditLifecycleTrace.write(\"indicator-appear size=\" + contentSizeCategory) }\n+            .onChange(of: dynamicTypeSize) { _, _ in\n+                AuditLifecycleTrace.write(\"text-size-change size=\" + contentSizeCategory)\n+            }\n     }\n }\n #endif\n@@ -123,17 +128,45 @@ final class PrivacySceneDelegate: NSObject, UIWindowSceneDelegate {\n     private var cover: UIWindow?\n \n     func sceneWillResignActive(_ scene: UIScene) {\n+        #if DEBUG\n+        AuditLifecycleTrace.write(\"scene-will-resign\")\n+        #endif\n         guard let windowScene = scene as? UIWindowScene else { return }\n         let cover = UIWindow(windowScene: windowScene)\n         cover.windowLevel = .alert + 1\n         cover.rootViewController = UIHostingController(rootView: PrivacyCoverView())\n         UIView.performWithoutAnimation { cover.isHidden = false }\n         self.cover = cover\n+        #if DEBUG\n+        AuditLifecycleTrace.write(\"privacy-cover-shown\")\n+        #endif\n     }\n \n     func sceneDidBecomeActive(_ scene: UIScene) {\n+        #if DEBUG\n+        AuditLifecycleTrace.write(\"scene-did-become-active\")\n+        #endif\n         guard let cover else { return }\n         UIView.performWithoutAnimation { cover.isHidden = true }\n         self.cover = nil\n+        #if DEBUG\n+        AuditLifecycleTrace.write(\"privacy-cover-removed\")\n+        #endif\n     }\n }\n+\n+\n+#if DEBUG\n+@MainActor\n+private enum AuditLifecycleTrace {\n+    private static let logger = Logger(subsystem: \"com.auditco.ace.audit-diagnostic\", category: \"lifecycle\")\n+\n+    static func write(_ event: String) {\n+        guard UITestScenario.current != nil else { return }\n+        let application = UIApplication.shared\n+        let windows = application.connectedScenes.compactMap { $0 as? UIWindowScene }.flatMap(\\.windows)\n+        let visibleLevels = windows.filter { !$0.isHidden }.map { String(describing: $0.windowLevel.rawValue) }.joined(separator: \",\")\n+        logger.notice(\"\\(event, privacy: .public) epoch=\\(Date().timeIntervalSince1970, privacy: .public) appState=\\(application.applicationState.rawValue, privacy: .public) visibleWindowLevels=\\(visibleLevels, privacy: .public)\")\n+    }\n+}\n+#endif\ndiff --git a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\nindex 175c95d..06b71b9 100644\n--- a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n+++ b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n@@ -973,6 +973,8 @@ final class ACEClientAppUITests: XCTestCase {\n     }\n \n     private func assertAccessibilityAudit(in app: XCUIApplication, scenario: String) throws {\n+        print(\"ACE_AUDIT_BEGIN scenario=\\(scenario) epoch=\\(Date().timeIntervalSince1970)\")\n+        defer { print(\"ACE_AUDIT_END scenario=\\(scenario) epoch=\\(Date().timeIntervalSince1970)\") }\n         let auditIssueHandler: @Sendable (XCUIAccessibilityAuditIssue) -> Bool = { issue in\n             // Keep bounded failure diagnostics for the approved controlled runner.\n             print(\"ACE_A11Y_ISSUE \\(Self.accessibilityIssueJSON(issue, scenario: scenario))\")\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/Developer/sqe-platform-audit-metadata",
      "HEAD": "1b38b90e9a949b7e73965602612c11be6f19e9fc",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientApp/ACEClientAppApp.swift b/ios/ACEClientApp/ACEClientApp/ACEClientAppApp.swift\nindex f3c6b6b..f8a9bce 100644\n--- a/ios/ACEClientApp/ACEClientApp/ACEClientAppApp.swift\n+++ b/ios/ACEClientApp/ACEClientApp/ACEClientAppApp.swift\n@@ -16,22 +16,34 @@ struct ACEClientApp: App {\n         default: return nil\n         }\n     }\n+\n+    private var showsUITestObservation: Bool {\n+        UITestScenario.current != nil\n+            && ProcessInfo.processInfo.environment[\"ACE_UI_TEST_SHOW_DIAGNOSTICS\"] != \"0\"\n+    }\n+\n+    @ViewBuilder\n+    private var rootContent: some View {\n+        if showsUITestObservation {\n+            RootView(state: state)\n+                .modifier(UITestObservationMetadata())\n+        } else {\n+            RootView(state: state)\n+        }\n+    }\n     #endif\n \n     var body: some Scene {\n         WindowGroup {\n-            RootView(state: state)\n-            #if DEBUG\n-                .safeAreaInset(edge: .top, spacing: 0) {\n-                    // Manual phone previews can omit test metadata. Audit runs retain it.\n-                    if UITestScenario.current != nil,\n-                       ProcessInfo.processInfo.environment[\"ACE_UI_TEST_SHOW_DIAGNOSTICS\"] != \"0\" {\n-                        EffectiveInterfaceStyleIndicator()\n-                    }\n-                }\n-                .preferredColorScheme(uiTestColorScheme)\n-            #endif\n-                .task {\n+            Group {\n+                #if DEBUG\n+                rootContent\n+                    .preferredColorScheme(uiTestColorScheme)\n+                #else\n+                RootView(state: state)\n+                #endif\n+            }\n+            .task {\n                 #if DEBUG\n                 guard UITestScenario.current == nil else { return }\n                 #endif\n@@ -42,7 +54,7 @@ struct ACEClientApp: App {\n }\n \n #if DEBUG\n-private struct EffectiveInterfaceStyleIndicator: View {\n+private struct UITestObservationMetadata: ViewModifier {\n     // Read the window's environment, not the App's scene-level environment.\n     @Environment(\\.colorScheme) private var colorScheme\n     @Environment(\\.dynamicTypeSize) private var dynamicTypeSize\n@@ -95,15 +107,11 @@ private struct EffectiveInterfaceStyleIndicator: View {\n         return window.bounds.width > window.bounds.height ? \"landscape\" : \"portrait\"\n     }\n \n-    var body: some View {\n-        Text(colorScheme == .dark ? \"dark\" : \"light\")\n-            .font(.body)\n-            .foregroundStyle(.primary)\n-            .frame(maxWidth: .infinity, alignment: .leading)\n-            .padding(.horizontal)\n-            .padding(.vertical, 8)\n-            .background(.background)\n+    func body(content: Content) -> some View {\n+        content\n+            .accessibilityElement(children: .contain)\n             .accessibilityIdentifier(\"Effective interface style\")\n+            .accessibilityLabel(colorScheme == .dark ? \"dark\" : \"light\")\n             .accessibilityValue(accessibilityValue)\n     }\n }\ndiff --git a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\nindex 175c95d..3e2af3c 100644\n--- a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n+++ b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n@@ -142,8 +142,14 @@ final class ACEClientAppUITests: XCTestCase {\n         return app\n     }\n \n+    private func observationElement(in app: XCUIApplication) -> XCUIElement {\n+        app.descendants(matching: .any)\n+            .matching(identifier: \"Effective interface style\")\n+            .firstMatch\n+    }\n+\n     private func observedSettings(in app: XCUIApplication) -> CoverageSettings? {\n-        let indicator = app.staticTexts[\"Effective interface style\"]\n+        let indicator = observationElement(in: app)\n         guard requireCoverage(indicator.waitForExistence(timeout: 5), \"Coverage observation indicator must exist\") else { return nil }\n         let ready = XCTNSPredicateExpectation(predicate: NSPredicate { _, _ in\n             guard let value = indicator.value as? String, let data = value.data(using: .utf8) else { return false }\n@@ -618,7 +624,7 @@ final class ACEClientAppUITests: XCTestCase {\n     func testBothAppearances() {\n         for appearance in [\"light\", \"dark\"] {\n             let app = launch(\"release\", appearance: appearance)\n-            let indicator = app.staticTexts[\"Effective interface style\"]\n+            let indicator = observationElement(in: app)\n             XCTAssertTrue(indicator.waitForExistence(timeout: 5), \"Appearance indicator must exist\")\n             let displayedAppearance = XCTNSPredicateExpectation(\n                 predicate: NSPredicate(format: \"label == %@\", appearance), object: indicator\n@@ -663,7 +669,7 @@ final class ACEClientAppUITests: XCTestCase {\n         XCTAssertEqual(copyButtons.count, approvedCopyControls.count, \"The release screen must expose only one grouped action copy control\")\n         assertMinimumActionTargets(in: app)\n         let appearance = requiredAppearance()\n-        let indicator = app.staticTexts[\"Effective interface style\"]\n+        let indicator = observationElement(in: app)\n         XCTAssertTrue(indicator.waitForExistence(timeout: 5), \"Appearance indicator must exist\")\n         let displayedAppearance = XCTNSPredicateExpectation(\n             predicate: NSPredicate(format: \"label == %@\", appearance), object: indicator\n@@ -805,7 +811,7 @@ final class ACEClientAppUITests: XCTestCase {\n         XCUIDevice.shared.orientation = .portrait\n         let app = launchWithNormalDeviceSettings(\"release\")\n         defer { app.terminate() }\n-        let indicator = app.staticTexts[\"Effective interface style\"]\n+        let indicator = observationElement(in: app)\n         XCTAssertTrue(indicator.waitForExistence(timeout: 5), \"Appearance indicator must exist\")\n         let displayedAppearance = XCTNSPredicateExpectation(\n             predicate: NSPredicate(format: \"label == %@\", expectedAppearance), object: indicator\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/Developer/sqe-platform-audit-metadata-control",
      "HEAD": "1b38b90e9a949b7e73965602612c11be6f19e9fc",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\nindex 175c95d..3e2af3c 100644\n--- a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n+++ b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n@@ -142,8 +142,14 @@ final class ACEClientAppUITests: XCTestCase {\n         return app\n     }\n \n+    private func observationElement(in app: XCUIApplication) -> XCUIElement {\n+        app.descendants(matching: .any)\n+            .matching(identifier: \"Effective interface style\")\n+            .firstMatch\n+    }\n+\n     private func observedSettings(in app: XCUIApplication) -> CoverageSettings? {\n-        let indicator = app.staticTexts[\"Effective interface style\"]\n+        let indicator = observationElement(in: app)\n         guard requireCoverage(indicator.waitForExistence(timeout: 5), \"Coverage observation indicator must exist\") else { return nil }\n         let ready = XCTNSPredicateExpectation(predicate: NSPredicate { _, _ in\n             guard let value = indicator.value as? String, let data = value.data(using: .utf8) else { return false }\n@@ -618,7 +624,7 @@ final class ACEClientAppUITests: XCTestCase {\n     func testBothAppearances() {\n         for appearance in [\"light\", \"dark\"] {\n             let app = launch(\"release\", appearance: appearance)\n-            let indicator = app.staticTexts[\"Effective interface style\"]\n+            let indicator = observationElement(in: app)\n             XCTAssertTrue(indicator.waitForExistence(timeout: 5), \"Appearance indicator must exist\")\n             let displayedAppearance = XCTNSPredicateExpectation(\n                 predicate: NSPredicate(format: \"label == %@\", appearance), object: indicator\n@@ -663,7 +669,7 @@ final class ACEClientAppUITests: XCTestCase {\n         XCTAssertEqual(copyButtons.count, approvedCopyControls.count, \"The release screen must expose only one grouped action copy control\")\n         assertMinimumActionTargets(in: app)\n         let appearance = requiredAppearance()\n-        let indicator = app.staticTexts[\"Effective interface style\"]\n+        let indicator = observationElement(in: app)\n         XCTAssertTrue(indicator.waitForExistence(timeout: 5), \"Appearance indicator must exist\")\n         let displayedAppearance = XCTNSPredicateExpectation(\n             predicate: NSPredicate(format: \"label == %@\", appearance), object: indicator\n@@ -805,7 +811,7 @@ final class ACEClientAppUITests: XCTestCase {\n         XCUIDevice.shared.orientation = .portrait\n         let app = launchWithNormalDeviceSettings(\"release\")\n         defer { app.terminate() }\n-        let indicator = app.staticTexts[\"Effective interface style\"]\n+        let indicator = observationElement(in: app)\n         XCTAssertTrue(indicator.waitForExistence(timeout: 5), \"Appearance indicator must exist\")\n         let displayedAppearance = XCTNSPredicateExpectation(\n             predicate: NSPredicate(format: \"label == %@\", expectedAppearance), object: indicator\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/Developer/sqe-platform-copy-colour-trial",
      "HEAD": "1b38b90e9a949b7e73965602612c11be6f19e9fc",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientApp/ACEClientAppApp.swift b/ios/ACEClientApp/ACEClientApp/ACEClientAppApp.swift\nindex f3c6b6b..f8a9bce 100644\n--- a/ios/ACEClientApp/ACEClientApp/ACEClientAppApp.swift\n+++ b/ios/ACEClientApp/ACEClientApp/ACEClientAppApp.swift\n@@ -16,22 +16,34 @@ struct ACEClientApp: App {\n         default: return nil\n         }\n     }\n+\n+    private var showsUITestObservation: Bool {\n+        UITestScenario.current != nil\n+            && ProcessInfo.processInfo.environment[\"ACE_UI_TEST_SHOW_DIAGNOSTICS\"] != \"0\"\n+    }\n+\n+    @ViewBuilder\n+    private var rootContent: some View {\n+        if showsUITestObservation {\n+            RootView(state: state)\n+                .modifier(UITestObservationMetadata())\n+        } else {\n+            RootView(state: state)\n+        }\n+    }\n     #endif\n \n     var body: some Scene {\n         WindowGroup {\n-            RootView(state: state)\n-            #if DEBUG\n-                .safeAreaInset(edge: .top, spacing: 0) {\n-                    // Manual phone previews can omit test metadata. Audit runs retain it.\n-                    if UITestScenario.current != nil,\n-                       ProcessInfo.processInfo.environment[\"ACE_UI_TEST_SHOW_DIAGNOSTICS\"] != \"0\" {\n-                        EffectiveInterfaceStyleIndicator()\n-                    }\n-                }\n-                .preferredColorScheme(uiTestColorScheme)\n-            #endif\n-                .task {\n+            Group {\n+                #if DEBUG\n+                rootContent\n+                    .preferredColorScheme(uiTestColorScheme)\n+                #else\n+                RootView(state: state)\n+                #endif\n+            }\n+            .task {\n                 #if DEBUG\n                 guard UITestScenario.current == nil else { return }\n                 #endif\n@@ -42,7 +54,7 @@ struct ACEClientApp: App {\n }\n \n #if DEBUG\n-private struct EffectiveInterfaceStyleIndicator: View {\n+private struct UITestObservationMetadata: ViewModifier {\n     // Read the window's environment, not the App's scene-level environment.\n     @Environment(\\.colorScheme) private var colorScheme\n     @Environment(\\.dynamicTypeSize) private var dynamicTypeSize\n@@ -95,15 +107,11 @@ private struct EffectiveInterfaceStyleIndicator: View {\n         return window.bounds.width > window.bounds.height ? \"landscape\" : \"portrait\"\n     }\n \n-    var body: some View {\n-        Text(colorScheme == .dark ? \"dark\" : \"light\")\n-            .font(.body)\n-            .foregroundStyle(.primary)\n-            .frame(maxWidth: .infinity, alignment: .leading)\n-            .padding(.horizontal)\n-            .padding(.vertical, 8)\n-            .background(.background)\n+    func body(content: Content) -> some View {\n+        content\n+            .accessibilityElement(children: .contain)\n             .accessibilityIdentifier(\"Effective interface style\")\n+            .accessibilityLabel(colorScheme == .dark ? \"dark\" : \"light\")\n             .accessibilityValue(accessibilityValue)\n     }\n }\ndiff --git a/ios/ACEClientApp/ACEClientApp/Views.swift b/ios/ACEClientApp/ACEClientApp/Views.swift\nindex 757be82..d613e14 100644\n--- a/ios/ACEClientApp/ACEClientApp/Views.swift\n+++ b/ios/ACEClientApp/ACEClientApp/Views.swift\n@@ -203,6 +203,7 @@ struct ReleaseView: View {\n             }\n             .frame(maxWidth: .infinity, alignment: .leading)\n         }\n+        .tint(.primary)\n     }\n }\n \n@@ -253,18 +254,12 @@ struct ValueRow: View {\n     let value: String\n \n     var body: some View {\n-        VStack(alignment: .leading, spacing: 6) {\n-            Text(field.label)\n-                .font(.subheadline)\n-                .foregroundStyle(.primary)\n-            Text(value)\n-                .font(.body)\n-                .frame(maxWidth: .infinity, alignment: .leading)\n-                .fixedSize(horizontal: false, vertical: true)\n-        }\n-        .frame(maxWidth: .infinity, alignment: .leading)\n-        .accessibilityElement(children: .combine)\n-        .accessibilityLabel(\"\\(field.label): \\(value)\")\n+        Text(\"\\(field.label)\\n\\(value)\")\n+            .font(.body)\n+            .foregroundStyle(.primary)\n+            .frame(maxWidth: .infinity, alignment: .leading)\n+            .fixedSize(horizontal: false, vertical: true)\n+            .accessibilityLabel(\"\\(field.label): \\(value)\")\n     }\n }\n \ndiff --git a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\nindex 175c95d..38d6c59 100644\n--- a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n+++ b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n@@ -142,8 +142,14 @@ final class ACEClientAppUITests: XCTestCase {\n         return app\n     }\n \n+    private func observationElement(in app: XCUIApplication) -> XCUIElement {\n+        app.descendants(matching: .any)\n+            .matching(identifier: \"Effective interface style\")\n+            .firstMatch\n+    }\n+\n     private func observedSettings(in app: XCUIApplication) -> CoverageSettings? {\n-        let indicator = app.staticTexts[\"Effective interface style\"]\n+        let indicator = observationElement(in: app)\n         guard requireCoverage(indicator.waitForExistence(timeout: 5), \"Coverage observation indicator must exist\") else { return nil }\n         let ready = XCTNSPredicateExpectation(predicate: NSPredicate { _, _ in\n             guard let value = indicator.value as? String, let data = value.data(using: .utf8) else { return false }\n@@ -618,7 +624,7 @@ final class ACEClientAppUITests: XCTestCase {\n     func testBothAppearances() {\n         for appearance in [\"light\", \"dark\"] {\n             let app = launch(\"release\", appearance: appearance)\n-            let indicator = app.staticTexts[\"Effective interface style\"]\n+            let indicator = observationElement(in: app)\n             XCTAssertTrue(indicator.waitForExistence(timeout: 5), \"Appearance indicator must exist\")\n             let displayedAppearance = XCTNSPredicateExpectation(\n                 predicate: NSPredicate(format: \"label == %@\", appearance), object: indicator\n@@ -663,7 +669,7 @@ final class ACEClientAppUITests: XCTestCase {\n         XCTAssertEqual(copyButtons.count, approvedCopyControls.count, \"The release screen must expose only one grouped action copy control\")\n         assertMinimumActionTargets(in: app)\n         let appearance = requiredAppearance()\n-        let indicator = app.staticTexts[\"Effective interface style\"]\n+        let indicator = observationElement(in: app)\n         XCTAssertTrue(indicator.waitForExistence(timeout: 5), \"Appearance indicator must exist\")\n         let displayedAppearance = XCTNSPredicateExpectation(\n             predicate: NSPredicate(format: \"label == %@\", appearance), object: indicator\n@@ -805,7 +811,7 @@ final class ACEClientAppUITests: XCTestCase {\n         XCUIDevice.shared.orientation = .portrait\n         let app = launchWithNormalDeviceSettings(\"release\")\n         defer { app.terminate() }\n-        let indicator = app.staticTexts[\"Effective interface style\"]\n+        let indicator = observationElement(in: app)\n         XCTAssertTrue(indicator.waitForExistence(timeout: 5), \"Appearance indicator must exist\")\n         let displayedAppearance = XCTNSPredicateExpectation(\n             predicate: NSPredicate(format: \"label == %@\", expectedAppearance), object: indicator\n@@ -946,20 +952,24 @@ final class ACEClientAppUITests: XCTestCase {\n         let after = releaseContrastProbeFrames(in: app)\n         print(\"ACE_MCX19_CONTRAST_PROBE \\(Self.releaseContrastProbeJSON(before: before, after: after))\")\n         XCTAssertEqual(after.actionHeader, before.actionHeader, \"Action 1 frame changed during the release contrast probe\")\n-        XCTAssertEqual(after.actionStatus, before.actionStatus, \"OPEN frame changed during the release contrast probe\")\n+        XCTAssertEqual(\n+            after.actionStatusField,\n+            before.actionStatusField,\n+            \"Action status: OPEN frame changed during the release contrast probe\"\n+        )\n     }\n \n-    private func releaseContrastProbeFrames(in app: XCUIApplication) -> (actionHeader: CGRect, actionStatus: CGRect) {\n-        (app.staticTexts[\"Action 1\"].frame, app.staticTexts[\"OPEN\"].frame)\n+    private func releaseContrastProbeFrames(in app: XCUIApplication) -> (actionHeader: CGRect, actionStatusField: CGRect) {\n+        (app.staticTexts[\"Action 1\"].frame, app.staticTexts[\"Action status: OPEN\"].frame)\n     }\n \n     private static func releaseContrastProbeJSON(\n-        before: (actionHeader: CGRect, actionStatus: CGRect),\n-        after: (actionHeader: CGRect, actionStatus: CGRect)\n+        before: (actionHeader: CGRect, actionStatusField: CGRect),\n+        after: (actionHeader: CGRect, actionStatusField: CGRect)\n     ) -> String {\n         let payload: [String: Any] = [\n-            \"after\": [\"Action 1\": frameJSON(after.actionHeader), \"OPEN\": frameJSON(after.actionStatus)],\n-            \"before\": [\"Action 1\": frameJSON(before.actionHeader), \"OPEN\": frameJSON(before.actionStatus)]\n+            \"after\": [\"Action 1\": frameJSON(after.actionHeader), \"Action status: OPEN\": frameJSON(after.actionStatusField)],\n+            \"before\": [\"Action 1\": frameJSON(before.actionHeader), \"Action status: OPEN\": frameJSON(before.actionStatusField)]\n         ]\n         guard let data = try? JSONSerialization.data(withJSONObject: payload, options: [.sortedKeys]),\n               let text = String(data: data, encoding: .utf8) else {\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/Developer/sqe-platform-d2-reflow-20260917",
      "HEAD": "52b212aca02190ee0e742debe383fa6016fe36b9",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\nindex 54db2a6..5a2446b 100644\n--- a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n+++ b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n@@ -1,6 +1,7 @@\n import Foundation\n import CoreGraphics\n import XCTest\n+import Synchronization\n \n final class ACEClientAppUITests: XCTestCase {\n     private struct CoverageSettings: Codable, Hashable {\n@@ -32,6 +33,189 @@ final class ACEClientAppUITests: XCTestCase {\n         let increaseContrast: Bool?\n     }\n \n+    // Temporary D2 diagnostic. Never part of an acceptance candidate.\n+    private final class D2Issues: Sendable {\n+        private let records = Mutex<[Data]>([])\n+        func append(_ data: Data) { records.withLock { $0.append(data) } }\n+        var snapshot: [Data] { records.withLock { $0 } }\n+    }\n+\n+    func testCopyReflowCheck() {\n+        let environment = ProcessInfo.processInfo.environment\n+        guard let arm = environment[\"ACE_D2_ARM\"], [\"X\", \"Y\"].contains(arm),\n+              let countText = environment[\"ACE_D2_MAX_CALLS\"], let maxCalls = Int(countText),\n+              [2, 30].contains(maxCalls), environment[\"ACE_UI_TEST_APPEARANCE\"] == nil else {\n+            XCTFail(\"D2 requires arm X/Y, 2/30 post-copy calls, and no appearance override\")\n+            return\n+        }\n+        let forceFailure = environment[\"ACE_D2_FORCE_FAIL\"] == \"1\"\n+        guard !forceFailure || (arm == \"Y\" && maxCalls == 2) else {\n+            XCTFail(\"D2 deliberate failure is permitted only in the two-call Y harness\")\n+            return\n+        }\n+        let previousContinue = continueAfterFailure\n+        continueAfterFailure = true\n+        let previousOrientation = XCUIDevice.shared.orientation\n+        defer {\n+            XCUIDevice.shared.orientation = previousOrientation\n+            continueAfterFailure = previousContinue\n+        }\n+        let expected = CoverageSettings(appearance: \"light\", contentSize: \"large\", orientation: \"portrait\",\n+                                        boldText: false, reduceMotion: false, increaseContrast: false)\n+        XCUIDevice.shared.orientation = .portrait\n+        let app = launchForCoverageObservation(scenario: \"copyConfirmation\")\n+        defer { app.terminate() }\n+        guard waitForCoverageState(in: app, scenario: \"copyConfirmation\"),\n+              assertRequestedOrientation(in: app, requested: \"portrait\"),\n+              let observed = observedSettings(in: app),\n+              requireCoverage(observed == expected, \"D2 observed settings differ: \\(observed.payload)\") else { return }\n+        emitJSON(marker: \"ACE_D2_SETTINGS\", payload: [\"observed\": observed.payload, \"arm\": arm])\n+        let caseID = \"pilot.iPhone-17.copyConfirmation.light.large.portrait.0.0.0\"\n+        var screenshots: [String] = []\n+        captureCoverageViewport(in: app, caseID: caseID, viewport: \"initial\", screenshots: &screenshots)\n+        let copy = app.buttons[\"Copy action 1\"]\n+        let scroll = app.scrollViews.firstMatch\n+        d2InitialAudit(in: app, copy: copy, scroll: scroll, arm: arm)\n+\n+        scrollUntilVisible(copy, in: app, field: \"Copy action 1\")\n+        guard verifyActionTarget(copy, name: \"Copy action 1\") else { return }\n+        copy.tap()\n+        let confirmation = coverageElement(in: app, labeled: \"Copied Action 1.\")\n+        guard requireCoverage(confirmation.waitForExistence(timeout: 5), \"Copy confirmation is missing\") else { return }\n+        scrollUntilVisible(confirmation, in: app, field: \"Copy confirmation\")\n+        guard requireCoverage(isFullyVisible(confirmation, in: scroll), \"Copy confirmation must be visible\") else { return }\n+        captureCoverageViewport(in: app, caseID: caseID, viewport: \"copy-confirmation\", screenshots: &screenshots)\n+\n+        for call in 1...maxCalls {\n+            if call >= 2 {\n+                drag(scroll, upward: true)\n+                Thread.sleep(forTimeInterval: 0.5)\n+            }\n+            if d2PostCopyAudit(in: app, copy: copy, scroll: scroll, arm: arm, call: call) { return }\n+            if forceFailure && call == 2 { XCTFail(\"D2 deliberate harness failure\") }\n+        }\n+    }\n+\n+    private func d2InitialAudit(in app: XCUIApplication, copy: XCUIElement, scroll: XCUIElement, arm: String) {\n+        let before = copy.frame\n+        let scrollBefore = scroll.frame\n+        let failuresBefore = testRun?.failureCount ?? 0\n+        let start = ProcessInfo.processInfo.systemUptime\n+        var helperReturned = false\n+        emitJSON(marker: \"ACE_D2_CALL_STARTED\", payload: [\"arm\": arm, \"call\": 0, \"uptime\": start])\n+        defer {\n+            let end = ProcessInfo.processInfo.systemUptime\n+            let after = copy.frame\n+            emitJSON(marker: \"ACE_D2_CALL\", payload: [\n+                \"arm\": arm, \"call\": 0, \"requestedAuditName\": \"all\", \"comparative\": false,\n+                \"preFrame\": Self.frameJSON(before), \"postFrame\": Self.frameJSON(after),\n+                \"scrollPreFrame\": Self.frameJSON(scrollBefore), \"scrollPostFrame\": Self.frameJSON(scroll.frame),\n+                \"deltaY\": Double(after.minY - before.minY), \"startUptime\": start, \"endUptime\": end,\n+                \"durationSeconds\": end - start, \"helperReturned\": helperReturned,\n+                \"nativeFailureCountDelta\": (testRun?.failureCount ?? 0) - failuresBefore,\n+                \"issueSourceMarker\": \"ACE_A11Y_ISSUE\", \"issues\": NSNull(), \"issueCount\": NSNull(),\n+                \"issueResolution\": \"Join unchanged helper issue lines between this call's start/end markers in retained native.log\",\n+                \"errorSource\": \"Unchanged helper retains thrown error in native assertion and log\",\n+                \"frameEvidence\": \"Settled positions only; not audit sample positions\"\n+            ])\n+        }\n+        // Keep the committed initial audit helper unchanged. Its issue JSON is joined externally.\n+        helperReturned = runCoverageAudit(in: app, scenario: \"copyConfirmation\", viewport: \"initial\")\n+    }\n+\n+    private func d2PostCopyAudit(in app: XCUIApplication, copy: XCUIElement, scroll: XCUIElement,\n+                               arm: String, call: Int) -> Bool {\n+        let before = copy.frame\n+        let scrollBefore = scroll.frame\n+        let start = ProcessInfo.processInfo.systemUptime\n+        let collector = D2Issues()\n+        var errorText: String?\n+        var stopReason: String?\n+        var shouldStop = false\n+        let auditName = arm == \"X\" ? \"all\" : \"all-minus-dynamicType\"\n+        emitJSON(marker: \"ACE_D2_CALL_STARTED\", payload: [\"arm\": arm, \"call\": call, \"uptime\": start])\n+        do {\n+        defer {\n+            let end = ProcessInfo.processInfo.systemUptime\n+            let after = copy.frame\n+            var issues: [[String: Any]] = []\n+            for data in collector.snapshot {\n+                do {\n+                    guard let issue = try JSONSerialization.jsonObject(with: data) as? [String: Any] else {\n+                        XCTFail(\"D2 issue payload is not an object\")\n+                        continue\n+                    }\n+                    issues.append(issue)\n+                } catch { XCTFail(\"D2 issue decoding failed: \\(error.localizedDescription)\") }\n+            }\n+            let delta = Double(after.minY - before.minY)\n+            if issues.contains(where: { issue in\n+                let element = issue[\"element\"] as? [String: Any]\n+                return (issue[\"isCopyContrast\"] as? Bool) == true\n+                    && [\"Copy action\", \"Copy action 1\"].contains(element?[\"label\"] as? String ?? \"\")\n+            }) { stopReason = \"Copy contrast finding\" }\n+            if arm == \"Y\" && delta != 0 { stopReason = stopReason ?? \"Y settled delta is non-zero\" }\n+            if errorText != nil { stopReason = stopReason ?? \"Thrown audit error\" }\n+            shouldStop = stopReason != nil\n+            var record: [String: Any] = [\n+                \"arm\": arm, \"call\": call, \"requestedAuditName\": auditName, \"comparative\": true,\n+                \"preFrame\": Self.frameJSON(before), \"postFrame\": Self.frameJSON(after),\n+                \"scrollPreFrame\": Self.frameJSON(scrollBefore), \"scrollPostFrame\": Self.frameJSON(scroll.frame),\n+                \"deltaY\": delta, \"startUptime\": start, \"endUptime\": end, \"durationSeconds\": end - start,\n+                \"issueCount\": issues.count, \"issues\": issues, \"addedDragAndHalfSecondDelay\": call >= 2,\n+                \"diagnosticOnly\": true, \"frameEvidence\": \"Settled positions only; not audit sample positions\"\n+            ]\n+            if let errorText { record[\"error\"] = errorText }\n+            emitJSON(marker: \"ACE_D2_CALL\", payload: record)\n+            if let stopReason { emitJSON(marker: \"ACE_D2_STOP\", payload: [\"arm\": arm, \"call\": call, \"reason\": stopReason]) }\n+        }\n+        let handler: @Sendable (XCUIAccessibilityAuditIssue) -> Bool = { issue in\n+            var payload = Self.d2IssuePayload(issue, auditName: auditName, auditStart: start,\n+                                              callbackElapsedSeconds: ProcessInfo.processInfo.systemUptime - start,\n+                                              repetition: \"one-launch\", viewport: \"copy-confirmation\", arm: arm)\n+            payload[\"call\"] = call\n+            payload[\"isCopyContrast\"] = issue.auditType.contains(.contrast)\n+                && [\"Copy action\", \"Copy action 1\"].contains(issue.element?.label ?? \"\")\n+            do { collector.append(try JSONSerialization.data(withJSONObject: payload, options: [.sortedKeys])) }\n+            catch { XCTFail(\"D2 issue encoding failed: \\(error.localizedDescription)\") }\n+            let attachment = XCTAttachment(screenshot: XCUIScreen.main.screenshot())\n+            let attachmentName = \"D2 \\(arm) call \\(call) callback\"\n+            attachment.name = attachmentName\n+            attachment.lifetime = .keepAlways\n+            XCTContext.runActivity(named: attachmentName) { $0.add(attachment) }\n+            return false\n+        }\n+        do {\n+            let types: XCUIAccessibilityAuditType = arm == \"X\" ? .all : XCUIAccessibilityAuditType.all.subtracting(.dynamicType)\n+            try app.performAccessibilityAudit(for: types, handler)\n+        } catch {\n+            errorText = error.localizedDescription\n+            XCTFail(\"D2 audit threw at call \\(call): \\(error.localizedDescription)\")\n+        }\n+        }\n+        return shouldStop\n+    }\n+\n+    private static func d2IssuePayload(_ issue: XCUIAccessibilityAuditIssue, auditName: String,\n+                                       auditStart: TimeInterval, callbackElapsedSeconds: TimeInterval,\n+                                       repetition: String, viewport: String, arm: String) -> [String: Any] {\n+        var element: [String: Any] = [\"identifier\": \"\", \"label\": \"\", \"type\": \"unavailable\"]\n+        if let auditedElement = issue.element {\n+            element[\"identifier\"] = limitedAuditText(auditedElement.identifier)\n+            element[\"label\"] = limitedAuditText(auditedElement.label)\n+            element[\"type\"] = limitedAuditText(String(describing: auditedElement.elementType))\n+            element[\"frame\"] = frameJSON(auditedElement.frame)\n+        }\n+        return [\"arm\": arm, \"auditStartUptimeSeconds\": auditStart,\n+                \"auditType\": limitedAuditText(String(describing: issue.auditType)),\n+                \"callbackElapsedSeconds\": callbackElapsedSeconds,\n+                \"callbackEvidence\": \"XCUIScreen screenshot observed when the callback ran; not the audit sample rendering\",\n+                \"compactDescription\": limitedAuditText(issue.compactDescription),\n+                \"detailedDescription\": limitedAuditText(issue.detailedDescription),\n+                \"auditTypeRawValue\": NSNumber(value: issue.auditType.rawValue), \"element\": element,\n+                \"requestedAuditName\": auditName, \"repetition\": repetition, \"viewport\": viewport]\n+    }\n+\n     func testCoverageBatch() {\n         continueAfterFailure = false\n         guard let cases = decodedCoverageCases(), !cases.isEmpty else { return }\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/Developer/sqe-platform-field-text-trial",
      "HEAD": "1b38b90e9a949b7e73965602612c11be6f19e9fc",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientApp/ACEClientAppApp.swift b/ios/ACEClientApp/ACEClientApp/ACEClientAppApp.swift\nindex f3c6b6b..f8a9bce 100644\n--- a/ios/ACEClientApp/ACEClientApp/ACEClientAppApp.swift\n+++ b/ios/ACEClientApp/ACEClientApp/ACEClientAppApp.swift\n@@ -16,22 +16,34 @@ struct ACEClientApp: App {\n         default: return nil\n         }\n     }\n+\n+    private var showsUITestObservation: Bool {\n+        UITestScenario.current != nil\n+            && ProcessInfo.processInfo.environment[\"ACE_UI_TEST_SHOW_DIAGNOSTICS\"] != \"0\"\n+    }\n+\n+    @ViewBuilder\n+    private var rootContent: some View {\n+        if showsUITestObservation {\n+            RootView(state: state)\n+                .modifier(UITestObservationMetadata())\n+        } else {\n+            RootView(state: state)\n+        }\n+    }\n     #endif\n \n     var body: some Scene {\n         WindowGroup {\n-            RootView(state: state)\n-            #if DEBUG\n-                .safeAreaInset(edge: .top, spacing: 0) {\n-                    // Manual phone previews can omit test metadata. Audit runs retain it.\n-                    if UITestScenario.current != nil,\n-                       ProcessInfo.processInfo.environment[\"ACE_UI_TEST_SHOW_DIAGNOSTICS\"] != \"0\" {\n-                        EffectiveInterfaceStyleIndicator()\n-                    }\n-                }\n-                .preferredColorScheme(uiTestColorScheme)\n-            #endif\n-                .task {\n+            Group {\n+                #if DEBUG\n+                rootContent\n+                    .preferredColorScheme(uiTestColorScheme)\n+                #else\n+                RootView(state: state)\n+                #endif\n+            }\n+            .task {\n                 #if DEBUG\n                 guard UITestScenario.current == nil else { return }\n                 #endif\n@@ -42,7 +54,7 @@ struct ACEClientApp: App {\n }\n \n #if DEBUG\n-private struct EffectiveInterfaceStyleIndicator: View {\n+private struct UITestObservationMetadata: ViewModifier {\n     // Read the window's environment, not the App's scene-level environment.\n     @Environment(\\.colorScheme) private var colorScheme\n     @Environment(\\.dynamicTypeSize) private var dynamicTypeSize\n@@ -95,15 +107,11 @@ private struct EffectiveInterfaceStyleIndicator: View {\n         return window.bounds.width > window.bounds.height ? \"landscape\" : \"portrait\"\n     }\n \n-    var body: some View {\n-        Text(colorScheme == .dark ? \"dark\" : \"light\")\n-            .font(.body)\n-            .foregroundStyle(.primary)\n-            .frame(maxWidth: .infinity, alignment: .leading)\n-            .padding(.horizontal)\n-            .padding(.vertical, 8)\n-            .background(.background)\n+    func body(content: Content) -> some View {\n+        content\n+            .accessibilityElement(children: .contain)\n             .accessibilityIdentifier(\"Effective interface style\")\n+            .accessibilityLabel(colorScheme == .dark ? \"dark\" : \"light\")\n             .accessibilityValue(accessibilityValue)\n     }\n }\ndiff --git a/ios/ACEClientApp/ACEClientApp/Views.swift b/ios/ACEClientApp/ACEClientApp/Views.swift\nindex 757be82..c55b5e2 100644\n--- a/ios/ACEClientApp/ACEClientApp/Views.swift\n+++ b/ios/ACEClientApp/ACEClientApp/Views.swift\n@@ -253,18 +253,12 @@ struct ValueRow: View {\n     let value: String\n \n     var body: some View {\n-        VStack(alignment: .leading, spacing: 6) {\n-            Text(field.label)\n-                .font(.subheadline)\n-                .foregroundStyle(.primary)\n-            Text(value)\n-                .font(.body)\n-                .frame(maxWidth: .infinity, alignment: .leading)\n-                .fixedSize(horizontal: false, vertical: true)\n-        }\n-        .frame(maxWidth: .infinity, alignment: .leading)\n-        .accessibilityElement(children: .combine)\n-        .accessibilityLabel(\"\\(field.label): \\(value)\")\n+        Text(\"\\(field.label)\\n\\(value)\")\n+            .font(.body)\n+            .foregroundStyle(.primary)\n+            .frame(maxWidth: .infinity, alignment: .leading)\n+            .fixedSize(horizontal: false, vertical: true)\n+            .accessibilityLabel(\"\\(field.label): \\(value)\")\n     }\n }\n \ndiff --git a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\nindex 175c95d..38d6c59 100644\n--- a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n+++ b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n@@ -142,8 +142,14 @@ final class ACEClientAppUITests: XCTestCase {\n         return app\n     }\n \n+    private func observationElement(in app: XCUIApplication) -> XCUIElement {\n+        app.descendants(matching: .any)\n+            .matching(identifier: \"Effective interface style\")\n+            .firstMatch\n+    }\n+\n     private func observedSettings(in app: XCUIApplication) -> CoverageSettings? {\n-        let indicator = app.staticTexts[\"Effective interface style\"]\n+        let indicator = observationElement(in: app)\n         guard requireCoverage(indicator.waitForExistence(timeout: 5), \"Coverage observation indicator must exist\") else { return nil }\n         let ready = XCTNSPredicateExpectation(predicate: NSPredicate { _, _ in\n             guard let value = indicator.value as? String, let data = value.data(using: .utf8) else { return false }\n@@ -618,7 +624,7 @@ final class ACEClientAppUITests: XCTestCase {\n     func testBothAppearances() {\n         for appearance in [\"light\", \"dark\"] {\n             let app = launch(\"release\", appearance: appearance)\n-            let indicator = app.staticTexts[\"Effective interface style\"]\n+            let indicator = observationElement(in: app)\n             XCTAssertTrue(indicator.waitForExistence(timeout: 5), \"Appearance indicator must exist\")\n             let displayedAppearance = XCTNSPredicateExpectation(\n                 predicate: NSPredicate(format: \"label == %@\", appearance), object: indicator\n@@ -663,7 +669,7 @@ final class ACEClientAppUITests: XCTestCase {\n         XCTAssertEqual(copyButtons.count, approvedCopyControls.count, \"The release screen must expose only one grouped action copy control\")\n         assertMinimumActionTargets(in: app)\n         let appearance = requiredAppearance()\n-        let indicator = app.staticTexts[\"Effective interface style\"]\n+        let indicator = observationElement(in: app)\n         XCTAssertTrue(indicator.waitForExistence(timeout: 5), \"Appearance indicator must exist\")\n         let displayedAppearance = XCTNSPredicateExpectation(\n             predicate: NSPredicate(format: \"label == %@\", appearance), object: indicator\n@@ -805,7 +811,7 @@ final class ACEClientAppUITests: XCTestCase {\n         XCUIDevice.shared.orientation = .portrait\n         let app = launchWithNormalDeviceSettings(\"release\")\n         defer { app.terminate() }\n-        let indicator = app.staticTexts[\"Effective interface style\"]\n+        let indicator = observationElement(in: app)\n         XCTAssertTrue(indicator.waitForExistence(timeout: 5), \"Appearance indicator must exist\")\n         let displayedAppearance = XCTNSPredicateExpectation(\n             predicate: NSPredicate(format: \"label == %@\", expectedAppearance), object: indicator\n@@ -946,20 +952,24 @@ final class ACEClientAppUITests: XCTestCase {\n         let after = releaseContrastProbeFrames(in: app)\n         print(\"ACE_MCX19_CONTRAST_PROBE \\(Self.releaseContrastProbeJSON(before: before, after: after))\")\n         XCTAssertEqual(after.actionHeader, before.actionHeader, \"Action 1 frame changed during the release contrast probe\")\n-        XCTAssertEqual(after.actionStatus, before.actionStatus, \"OPEN frame changed during the release contrast probe\")\n+        XCTAssertEqual(\n+            after.actionStatusField,\n+            before.actionStatusField,\n+            \"Action status: OPEN frame changed during the release contrast probe\"\n+        )\n     }\n \n-    private func releaseContrastProbeFrames(in app: XCUIApplication) -> (actionHeader: CGRect, actionStatus: CGRect) {\n-        (app.staticTexts[\"Action 1\"].frame, app.staticTexts[\"OPEN\"].frame)\n+    private func releaseContrastProbeFrames(in app: XCUIApplication) -> (actionHeader: CGRect, actionStatusField: CGRect) {\n+        (app.staticTexts[\"Action 1\"].frame, app.staticTexts[\"Action status: OPEN\"].frame)\n     }\n \n     private static func releaseContrastProbeJSON(\n-        before: (actionHeader: CGRect, actionStatus: CGRect),\n-        after: (actionHeader: CGRect, actionStatus: CGRect)\n+        before: (actionHeader: CGRect, actionStatusField: CGRect),\n+        after: (actionHeader: CGRect, actionStatusField: CGRect)\n     ) -> String {\n         let payload: [String: Any] = [\n-            \"after\": [\"Action 1\": frameJSON(after.actionHeader), \"OPEN\": frameJSON(after.actionStatus)],\n-            \"before\": [\"Action 1\": frameJSON(before.actionHeader), \"OPEN\": frameJSON(before.actionStatus)]\n+            \"after\": [\"Action 1\": frameJSON(after.actionHeader), \"Action status: OPEN\": frameJSON(after.actionStatusField)],\n+            \"before\": [\"Action 1\": frameJSON(before.actionHeader), \"Action status: OPEN\": frameJSON(before.actionStatusField)]\n         ]\n         guard let data = try? JSONSerialization.data(withJSONObject: payload, options: [.sortedKeys]),\n               let text = String(data: data, encoding: .utf8) else {\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/Developer/sqe-platform-native-list-control",
      "HEAD": "1b38b90e9a949b7e73965602612c11be6f19e9fc",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\nindex 175c95d..cacd682 100644\n--- a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n+++ b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n@@ -657,10 +657,6 @@ final class ACEClientAppUITests: XCTestCase {\n             \"Copy Conclusion title\", \"Copy Conclusion summary\", \"Copy Evidence reference\", \"Copy Action description\",\n             \"Copy Action owner\", \"Copy Action target date\", \"Copy Action status\"\n         ]\n-        for identifier in approvedCopyControls { XCTAssertTrue(app.buttons[identifier].exists, identifier) }\n-        for identifier in removedIndividualCopyControls { XCTAssertFalse(app.buttons[identifier].exists, identifier) }\n-        let copyButtons = app.buttons.matching(NSPredicate(format: \"label BEGINSWITH %@\", \"Copy \"))\n-        XCTAssertEqual(copyButtons.count, approvedCopyControls.count, \"The release screen must expose only one grouped action copy control\")\n         assertMinimumActionTargets(in: app)\n         let appearance = requiredAppearance()\n         let indicator = app.staticTexts[\"Effective interface style\"]\n@@ -678,6 +674,11 @@ final class ACEClientAppUITests: XCTestCase {\n         }\n         try assertAccessibilityAudit(in: app, scenario: \"release-initial\")\n         assertFullReleaseInformation(in: app, appearance: appearance)\n+        // Native List creates rows on demand. Check the action controls after traversing every field.\n+        for identifier in approvedCopyControls { XCTAssertTrue(app.buttons[identifier].exists, identifier) }\n+        for identifier in removedIndividualCopyControls { XCTAssertFalse(app.buttons[identifier].exists, identifier) }\n+        let copyButtons = app.buttons.matching(NSPredicate(format: \"label BEGINSWITH %@\", \"Copy \"))\n+        XCTAssertEqual(copyButtons.count, approvedCopyControls.count, \"The release screen must expose only one grouped action copy control\")\n         assertMinimumActionTargets(in: app)\n         addScreenshot(of: app, named: \"Fictional release \u2014 approved-controls \u2014 \\(appearance)\")\n         assertReleaseContrastProbeFramesAreStable(in: app)\n@@ -856,8 +857,16 @@ final class ACEClientAppUITests: XCTestCase {\n         }\n     }\n \n-    private func scrollUntilVisible(_ element: XCUIElement, in app: XCUIApplication, field: String) {\n+    private func releaseScrollContainer(in app: XCUIApplication) -> XCUIElement {\n         let scrollView = app.scrollViews.firstMatch\n+        if scrollView.exists { return scrollView }\n+        let collectionView = app.collectionViews.firstMatch\n+        if collectionView.exists { return collectionView }\n+        return app.tables.firstMatch\n+    }\n+\n+    private func scrollUntilVisible(_ element: XCUIElement, in app: XCUIApplication, field: String) {\n+        let scrollView = releaseScrollContainer(in: app)\n         guard scrollView.waitForExistence(timeout: 5) else {\n             XCTFail(\"Release scroll view must exist\")\n             return\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/Developer/sqe-platform-native-list-trial",
      "HEAD": "1b38b90e9a949b7e73965602612c11be6f19e9fc",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientApp/Views.swift b/ios/ACEClientApp/ACEClientApp/Views.swift\nindex 757be82..098212f 100644\n--- a/ios/ACEClientApp/ACEClientApp/Views.swift\n+++ b/ios/ACEClientApp/ACEClientApp/Views.swift\n@@ -150,43 +150,54 @@ struct ReleaseView: View {\n     let signOut: () -> Void\n \n     var body: some View {\n-        ScrollView {\n-            VStack(alignment: .leading, spacing: 20) {\n-                SectionHeader(\"Current Release\")\n+        List {\n+            Section {\n                 HandlingLabel()\n-                ReleaseCard {\n-                    Text(\"Release Details\")\n-                        .font(.headline)\n-                        .accessibilityAddTraits(.isHeader)\n-                    ValueRow(field: .engagementName, value: release.engagementName)\n-                    ValueRow(field: .reviewStatus, value: release.reviewStatus)\n-                    ValueRow(field: .releaseVersion, value: String(release.releaseVersion))\n-                    ValueRow(field: .publishedAt, value: release.publishedAt)\n-                }\n-                if let conclusion = release.conclusion {\n-                    ReleaseCard {\n-                        SectionHeader(\"Conclusion\")\n-                        ValueRow(field: .conclusionTitle, value: conclusion.title)\n-                        ValueRow(field: .conclusionSummary, value: conclusion.summary)\n-                        ValueRow(field: .evidenceReference, value: conclusion.evidenceReferenceID)\n-                    }\n+            } header: {\n+                SectionHeader(\"Current Release\")\n+            }\n+\n+            Section {\n+                ValueRow(field: .engagementName, value: release.engagementName)\n+                ValueRow(field: .reviewStatus, value: release.reviewStatus)\n+                ValueRow(field: .releaseVersion, value: String(release.releaseVersion))\n+                ValueRow(field: .publishedAt, value: release.publishedAt)\n+            } header: {\n+                Text(\"Release Details\")\n+                    .font(.headline)\n+                    .accessibilityAddTraits(.isHeader)\n+            }\n+\n+            if let conclusion = release.conclusion {\n+                Section {\n+                    ValueRow(field: .conclusionTitle, value: conclusion.title)\n+                    ValueRow(field: .conclusionSummary, value: conclusion.summary)\n+                    ValueRow(field: .evidenceReference, value: conclusion.evidenceReferenceID)\n+                } header: {\n+                    SectionHeader(\"Conclusion\")\n                 }\n-                if !release.actions.isEmpty {\n+            }\n+\n+            if !release.actions.isEmpty {\n+                Section {\n                     SectionHeader(\"Actions\")\n-                    ForEach(Array(release.actions.enumerated()), id: \\.offset) { index, action in\n-                        ReleaseCard {\n-                            Text(\"Action \\(index + 1)\")\n-                                .font(.headline)\n-                                .accessibilityAddTraits(.isHeader)\n-                            ValueRow(field: .actionDescription, value: action.description)\n-                            ValueRow(field: .actionOwner, value: action.owner)\n-                            ValueRow(field: .actionTargetDate, value: action.targetDate)\n-                            ValueRow(field: .actionStatus, value: action.status)\n-                            ActionCopyControl(action: action, index: index)\n-                        }\n-                        .accessibilityElement(children: .contain)\n+                }\n+                ForEach(Array(release.actions.enumerated()), id: \\.offset) { index, action in\n+                    Section {\n+                        ValueRow(field: .actionDescription, value: action.description)\n+                        ValueRow(field: .actionOwner, value: action.owner)\n+                        ValueRow(field: .actionTargetDate, value: action.targetDate)\n+                        ValueRow(field: .actionStatus, value: action.status)\n+                        ActionCopyControl(action: action, index: index)\n+                    } header: {\n+                        Text(\"Action \\(index + 1)\")\n+                            .font(.headline)\n+                            .accessibilityAddTraits(.isHeader)\n                     }\n                 }\n+            }\n+\n+            Section {\n                 ForEach(notices, id: \\.self) { notice in Text(notice) }\n                 Button { refresh() } label: {\n                     Text(\"Refresh\")\n@@ -201,25 +212,8 @@ struct ReleaseView: View {\n                 }\n                 .accessibilityLabel(\"Sign out\")\n             }\n-            .frame(maxWidth: .infinity, alignment: .leading)\n         }\n-    }\n-}\n-\n-private struct ReleaseCard<Content: View>: View {\n-    let content: Content\n-\n-    init(@ViewBuilder content: () -> Content) {\n-        self.content = content()\n-    }\n-\n-    var body: some View {\n-        VStack(alignment: .leading, spacing: 12) {\n-            content\n-        }\n-        .frame(maxWidth: .infinity, alignment: .leading)\n-        .padding()\n-        .background(Color(uiColor: .secondarySystemBackground), in: RoundedRectangle(cornerRadius: 12))\n+        .listStyle(.insetGrouped)\n     }\n }\n \ndiff --git a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\nindex 175c95d..cacd682 100644\n--- a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n+++ b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n@@ -657,10 +657,6 @@ final class ACEClientAppUITests: XCTestCase {\n             \"Copy Conclusion title\", \"Copy Conclusion summary\", \"Copy Evidence reference\", \"Copy Action description\",\n             \"Copy Action owner\", \"Copy Action target date\", \"Copy Action status\"\n         ]\n-        for identifier in approvedCopyControls { XCTAssertTrue(app.buttons[identifier].exists, identifier) }\n-        for identifier in removedIndividualCopyControls { XCTAssertFalse(app.buttons[identifier].exists, identifier) }\n-        let copyButtons = app.buttons.matching(NSPredicate(format: \"label BEGINSWITH %@\", \"Copy \"))\n-        XCTAssertEqual(copyButtons.count, approvedCopyControls.count, \"The release screen must expose only one grouped action copy control\")\n         assertMinimumActionTargets(in: app)\n         let appearance = requiredAppearance()\n         let indicator = app.staticTexts[\"Effective interface style\"]\n@@ -678,6 +674,11 @@ final class ACEClientAppUITests: XCTestCase {\n         }\n         try assertAccessibilityAudit(in: app, scenario: \"release-initial\")\n         assertFullReleaseInformation(in: app, appearance: appearance)\n+        // Native List creates rows on demand. Check the action controls after traversing every field.\n+        for identifier in approvedCopyControls { XCTAssertTrue(app.buttons[identifier].exists, identifier) }\n+        for identifier in removedIndividualCopyControls { XCTAssertFalse(app.buttons[identifier].exists, identifier) }\n+        let copyButtons = app.buttons.matching(NSPredicate(format: \"label BEGINSWITH %@\", \"Copy \"))\n+        XCTAssertEqual(copyButtons.count, approvedCopyControls.count, \"The release screen must expose only one grouped action copy control\")\n         assertMinimumActionTargets(in: app)\n         addScreenshot(of: app, named: \"Fictional release \u2014 approved-controls \u2014 \\(appearance)\")\n         assertReleaseContrastProbeFramesAreStable(in: app)\n@@ -856,8 +857,16 @@ final class ACEClientAppUITests: XCTestCase {\n         }\n     }\n \n-    private func scrollUntilVisible(_ element: XCUIElement, in app: XCUIApplication, field: String) {\n+    private func releaseScrollContainer(in app: XCUIApplication) -> XCUIElement {\n         let scrollView = app.scrollViews.firstMatch\n+        if scrollView.exists { return scrollView }\n+        let collectionView = app.collectionViews.firstMatch\n+        if collectionView.exists { return collectionView }\n+        return app.tables.firstMatch\n+    }\n+\n+    private func scrollUntilVisible(_ element: XCUIElement, in app: XCUIApplication, field: String) {\n+        let scrollView = releaseScrollContainer(in: app)\n         guard scrollView.waitForExistence(timeout: 5) else {\n             XCTFail(\"Release scroll view must exist\")\n             return\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/Developer/sqe-platform-r5-diagnostic",
      "HEAD": "52b212aca02190ee0e742debe383fa6016fe36b9",
      "detached": true,
      "diff": "diff --git a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\nindex 54db2a6..ebf1834 100644\n--- a/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n+++ b/ios/ACEClientApp/ACEClientAppUITests/ACEClientAppUITests.swift\n@@ -1,5 +1,6 @@\n import Foundation\n import CoreGraphics\n+import Synchronization\n import XCTest\n \n final class ACEClientAppUITests: XCTestCase {\n@@ -32,6 +33,77 @@ final class ACEClientAppUITests: XCTestCase {\n         let increaseContrast: Bool?\n     }\n \n+    private enum D1Arm: String {\n+        case splitAudits = \"B\"\n+        case upperViewport = \"C\"\n+    }\n+\n+    private struct D1AuditConfiguration {\n+        let arm: D1Arm\n+        let repetition: String\n+    }\n+\n+    // Only this counter crosses into the audit callback; all access holds its mutex.\n+    private final class D1IssueCounter: Sendable {\n+        private let count = Mutex<Int>(0)\n+\n+        func recordIssue() { count.withLock { $0 += 1 } }\n+        var value: Int { count.withLock { $0 } }\n+    }\n+\n+    private final class D1AuditSession {\n+        let configuration: D1AuditConfiguration\n+        private let expectedAuditCallsPerViewport: Int\n+        private var attemptedAuditCalls = 0\n+        private var returnedNormallyAuditCalls = 0\n+        private var returnedNormallyCallsByViewport: [String: Int] = [:]\n+        let issueCounter = D1IssueCounter()\n+        private var auditErrors: [String] = []\n+\n+        init(configuration: D1AuditConfiguration) {\n+            self.configuration = configuration\n+            self.expectedAuditCallsPerViewport = configuration.arm == .splitAudits ? 7 : 1\n+        }\n+\n+        func recordAuditCompletion(viewport: String, returnedNormally: Bool) {\n+            attemptedAuditCalls += 1\n+            if returnedNormally {\n+                returnedNormallyAuditCalls += 1\n+                returnedNormallyCallsByViewport[viewport, default: 0] += 1\n+            }\n+        }\n+\n+        func recordAuditError(_ error: Error) {\n+            auditErrors.append(String(error.localizedDescription.prefix(256)))\n+        }\n+\n+        var callbackIssueCount: Int { issueCounter.value }\n+        var hasIssues: Bool { callbackIssueCount > 0 }\n+        var hasAuditErrors: Bool { !auditErrors.isEmpty }\n+\n+        func completionPayload() -> [String: Any] {\n+            let expectedViewports: Set<String> = [\"initial\", \"copy-confirmation\"]\n+            let completedViewports = Set(returnedNormallyCallsByViewport.compactMap {\n+                $0.value == expectedAuditCallsPerViewport ? $0.key : nil\n+            })\n+            let expectedAuditCalls = expectedAuditCallsPerViewport * expectedViewports.count\n+            return [\n+                \"arm\": configuration.arm.rawValue,\n+                \"repetition\": configuration.repetition,\n+                \"expectedAuditCalls\": expectedAuditCalls,\n+                \"attemptedAuditCalls\": attemptedAuditCalls,\n+                \"returnedNormallyAuditCalls\": returnedNormallyAuditCalls,\n+                \"completedViewports\": completedViewports.sorted(),\n+                \"bothViewportAuditGroupsCompleted\": completedViewports == expectedViewports,\n+                \"allAuditTypesAttempted\": attemptedAuditCalls == expectedAuditCalls,\n+                \"allAuditCallsReturnedNormally\": returnedNormallyAuditCalls == expectedAuditCalls,\n+                \"callbackIssueCount\": callbackIssueCount,\n+                \"auditErrors\": auditErrors\n+            ]\n+        }\n+    }\n+\n+    private var d1AuditSession: D1AuditSession?\n     func testCoverageBatch() {\n         continueAfterFailure = false\n         guard let cases = decodedCoverageCases(), !cases.isEmpty else { return }\n@@ -104,6 +176,35 @@ final class ACEClientAppUITests: XCTestCase {\n     }\n \n     private func runCoverageCase(_ coverageCase: CoverageCase) -> Bool {\n+        let d1Requested = ProcessInfo.processInfo.environment[\"ACE_D1_ARM\"] != nil\n+            || ProcessInfo.processInfo.environment[\"ACE_D1_REPETITION\"] != nil\n+        let d1Configuration = d1AuditConfiguration()\n+        guard !d1Requested || d1Configuration != nil else { return false }\n+\n+        let activeD1Session: D1AuditSession?\n+        let originalContinueAfterFailure: Bool?\n+        if let d1Configuration {\n+            guard coverageCase.scenario == \"copyConfirmation\" && coverageCase.audit else {\n+                XCTFail(\"D1 requires one audited copyConfirmation coverage case\")\n+                return false\n+            }\n+            let session = D1AuditSession(configuration: d1Configuration)\n+            activeD1Session = session\n+            originalContinueAfterFailure = continueAfterFailure\n+            d1AuditSession = session\n+            // The handler still returns false. This only lets every approved D1 audit call finish.\n+            continueAfterFailure = true\n+        } else {\n+            activeD1Session = nil\n+            originalContinueAfterFailure = nil\n+        }\n+        defer {\n+            if let activeD1Session, let originalContinueAfterFailure {\n+                emitJSON(marker: \"ACE_D1_DIAGNOSTIC_COMPLETE\", payload: activeD1Session.completionPayload())\n+                continueAfterFailure = originalContinueAfterFailure\n+                d1AuditSession = nil\n+            }\n+        }\n         guard ProcessInfo.processInfo.environment[\"ACE_UI_TEST_APPEARANCE\"] == nil else {\n             XCTFail(\"Coverage runs must not set ACE_UI_TEST_APPEARANCE\")\n             return false\n@@ -128,6 +229,21 @@ final class ACEClientAppUITests: XCTestCase {\n         }\n         guard verifyCoverageScenario(coverageCase.scenario, in: app, caseID: coverageCase.id,\n                                      audit: coverageCase.audit, screenshots: &screenshots, audits: &audits) else { return false }\n+        if let session = d1AuditSession {\n+            let completion = session.completionPayload()\n+            guard completion[\"bothViewportAuditGroupsCompleted\"] as? Bool == true,\n+                  completion[\"allAuditTypesAttempted\"] as? Bool == true,\n+                  completion[\"allAuditCallsReturnedNormally\"] as? Bool == true else {\n+                XCTFail(\"D1 did not complete every required audit call\")\n+                return false\n+            }\n+            if session.hasAuditErrors {\n+                XCTFail(\"D1 accessibility audit call raised an error after completing the diagnostic sequence\")\n+                return false\n+            }\n+            // An issue handler returning false already recorded XCTest's native failure.\n+            guard !session.hasIssues else { return false }\n+        }\n         emitJSON(marker: \"ACE_CASE_RESULT\", payload: [\"id\": coverageCase.id, \"scenario\": coverageCase.scenario,\n             \"observed\": observed.payload, \"result\": \"passed\", \"screenshots\": screenshots, \"auditInvocations\": audits])\n         return true\n@@ -383,10 +499,183 @@ final class ACEClientAppUITests: XCTestCase {\n     }\n \n     private func runCoverageAudit(in app: XCUIApplication, scenario: String, viewport: String) -> Bool {\n+        if let session = d1AuditSession {\n+            return runD1AccessibilityAudit(in: app, viewport: viewport, session: session)\n+        }\n         do { try assertAccessibilityAudit(in: app, scenario: \"\\(scenario)-\\(viewport)\"); return true }\n         catch { XCTFail(\"Accessibility audit failed for \\(scenario) \\(viewport): \\(error.localizedDescription)\"); return false }\n     }\n \n+    private func d1AuditConfiguration() -> D1AuditConfiguration? {\n+        let environment = ProcessInfo.processInfo.environment\n+        guard let armValue = environment[\"ACE_D1_ARM\"] else {\n+            if environment[\"ACE_D1_REPETITION\"] != nil {\n+                XCTFail(\"ACE_D1_REPETITION requires ACE_D1_ARM\")\n+            }\n+            return nil\n+        }\n+        guard let arm = D1Arm(rawValue: armValue) else {\n+            XCTFail(\"ACE_D1_ARM must be B or C\")\n+            return nil\n+        }\n+        guard let repetition = environment[\"ACE_D1_REPETITION\"], !repetition.isEmpty else {\n+            XCTFail(\"ACE_D1_REPETITION is required for D1\")\n+            return nil\n+        }\n+        return D1AuditConfiguration(arm: arm, repetition: repetition)\n+    }\n+\n+    private func runD1AccessibilityAudit(in app: XCUIApplication, viewport: String,\n+                                         session: D1AuditSession) -> Bool {\n+        if session.configuration.arm == .upperViewport && viewport == \"copy-confirmation\"\n+            && !placeCopyActionInUpperViewport(in: app, repetition: session.configuration.repetition) {\n+            return false\n+        }\n+\n+        let auditCalls: [(String, XCUIAccessibilityAuditType)]\n+        switch session.configuration.arm {\n+        case .splitAudits:\n+            // The iPhoneSimulator 26.4 XCUIAccessibilityAuditTypes.h declaration fixes this order.\n+            auditCalls = [\n+                (\"contrast\", .contrast),\n+                (\"elementDetection\", .elementDetection),\n+                (\"hitRegion\", .hitRegion),\n+                (\"sufficientElementDescription\", .sufficientElementDescription),\n+                (\"dynamicType\", .dynamicType),\n+                (\"textClipped\", .textClipped),\n+                (\"trait\", .trait)\n+            ]\n+        case .upperViewport:\n+            auditCalls = [(\"all\", .all)]\n+        }\n+\n+        for (auditName, auditType) in auditCalls {\n+            let auditStart = ProcessInfo.processInfo.systemUptime\n+            let issuesBeforeCall = session.callbackIssueCount\n+            let issueCounter = session.issueCounter\n+            let arm = session.configuration.arm.rawValue\n+            let repetition = session.configuration.repetition\n+            let issueHandler: @Sendable (XCUIAccessibilityAuditIssue) -> Bool = { issue in\n+                issueCounter.recordIssue()\n+                let elapsedSeconds = ProcessInfo.processInfo.systemUptime - auditStart\n+                let attachment = XCTAttachment(screenshot: XCUIScreen.main.screenshot())\n+                let attachmentName = \"D1 \\(arm) repetition \\(repetition) \\(viewport) \\(auditName) callback-time evidence\"\n+                attachment.name = attachmentName\n+                attachment.lifetime = .keepAlways\n+                XCTContext.runActivity(named: attachmentName) { activity in\n+                    activity.add(attachment)\n+                }\n+                Self.emitD1Issue(Self.d1IssuePayload(\n+                    issue,\n+                    auditName: auditName,\n+                    auditStart: auditStart,\n+                    callbackElapsedSeconds: elapsedSeconds,\n+                    repetition: repetition,\n+                    viewport: viewport,\n+                    arm: arm\n+                ))\n+                // Preserve XCTest's native audit finding without filtering it.\n+                return false\n+            }\n+            var returnedNormally = false\n+            var errorText: String?\n+            do {\n+                try app.performAccessibilityAudit(for: auditType, issueHandler)\n+                returnedNormally = true\n+            } catch {\n+                session.recordAuditError(error)\n+                errorText = String(error.localizedDescription.prefix(256))\n+                emitJSON(marker: \"ACE_D1_AUDIT_ERROR\", payload: [\n+                    \"arm\": session.configuration.arm.rawValue,\n+                    \"auditName\": auditName,\n+                    \"repetition\": session.configuration.repetition,\n+                    \"viewport\": viewport,\n+                    \"error\": errorText ?? \"\"\n+                ])\n+            }\n+            session.recordAuditCompletion(viewport: viewport, returnedNormally: returnedNormally)\n+            emitJSON(marker: \"ACE_D1_AUDIT_CALL\", payload: [\n+                \"arm\": session.configuration.arm.rawValue,\n+                \"auditName\": auditName,\n+                \"error\": errorText ?? NSNull(),\n+                \"issueObserved\": session.callbackIssueCount > issuesBeforeCall,\n+                \"repetition\": session.configuration.repetition,\n+                \"returnedNormally\": returnedNormally,\n+                \"viewport\": viewport\n+            ])\n+        }\n+        return true\n+    }\n+\n+    private func placeCopyActionInUpperViewport(in app: XCUIApplication, repetition: String) -> Bool {\n+        let copyAction = app.buttons[\"Copy action 1\"]\n+        let scrollView = app.scrollViews.firstMatch\n+        guard copyAction.exists && scrollView.exists else {\n+            XCTFail(\"D1 C requires the Copy action 1 button and release scroll view\")\n+            return false\n+        }\n+        for _ in 0..<16 {\n+            let copyFrame = copyAction.frame\n+            let viewport = scrollView.frame\n+            let midpoint = viewport.minY + viewport.height / 2\n+            if isFullyVisible(copyAction, in: scrollView) && copyFrame.maxY < midpoint { break }\n+            drag(scrollView, upward: copyFrame.maxY >= midpoint)\n+        }\n+        let firstFrame = copyAction.frame\n+        let viewport = scrollView.frame\n+        let midpoint = viewport.minY + viewport.height / 2\n+        guard requireCoverage(isFullyVisible(copyAction, in: scrollView) && firstFrame.maxY < midpoint,\n+                              \"D1 C could not place Copy action 1 fully above the scroll viewport midpoint\") else { return false }\n+        let frameReadInterval: TimeInterval = 0.5\n+        Thread.sleep(forTimeInterval: frameReadInterval)\n+        let secondFrame = copyAction.frame\n+        emitJSON(marker: \"ACE_D1_C_PLACEMENT\", payload: [\n+            \"firstFrame\": Self.frameJSON(firstFrame),\n+            \"midpoint\": Double(midpoint),\n+            \"repetition\": repetition,\n+            \"secondFrame\": Self.frameJSON(secondFrame),\n+            \"twoFrameReadsIdentical\": firstFrame == secondFrame,\n+            \"viewport\": Self.frameJSON(viewport)\n+        ])\n+        return requireCoverage(firstFrame == secondFrame,\n+                               \"D1 C Copy action 1 frame changed during the approved 0.5 second interval\")\n+    }\n+\n+    private static func d1IssuePayload(_ issue: XCUIAccessibilityAuditIssue, auditName: String, auditStart: TimeInterval,\n+                                callbackElapsedSeconds: TimeInterval, repetition: String, viewport: String, arm: String) -> [String: Any] {\n+        var element: [String: Any] = [\"identifier\": \"\", \"label\": \"\", \"type\": \"unavailable\"]\n+        if let auditedElement = issue.element {\n+            element[\"identifier\"] = Self.limitedAuditText(auditedElement.identifier)\n+            element[\"label\"] = Self.limitedAuditText(auditedElement.label)\n+            element[\"type\"] = Self.limitedAuditText(String(describing: auditedElement.elementType))\n+            let frame = auditedElement.frame\n+            element[\"frame\"] = Self.frameJSON(frame)\n+        }\n+        return [\n+            \"arm\": arm,\n+            \"auditStartUptimeSeconds\": auditStart,\n+            \"auditType\": Self.limitedAuditText(String(describing: issue.auditType)),\n+            \"callbackElapsedSeconds\": callbackElapsedSeconds,\n+            \"callbackEvidence\": \"XCUIScreen screenshot observed when the callback ran; not the audit sample rendering\",\n+            \"compactDescription\": Self.limitedAuditText(issue.compactDescription),\n+            \"detailedDescription\": Self.limitedAuditText(issue.detailedDescription),\n+            \"auditTypeRawValue\": NSNumber(value: issue.auditType.rawValue),\n+            \"element\": element,\n+            \"requestedAuditName\": auditName,\n+            \"repetition\": repetition,\n+            \"viewport\": viewport\n+        ]\n+    }\n+\n+    private static func emitD1Issue(_ payload: [String: Any]) {\n+        guard let data = try? JSONSerialization.data(withJSONObject: payload, options: [.sortedKeys]),\n+              let value = String(data: data, encoding: .utf8) else {\n+            XCTFail(\"Could not serialize ACE_D1_A11Y_ISSUE\")\n+            return\n+        }\n+        print(\"ACE_D1_A11Y_ISSUE \\(value)\")\n+    }\n+\n     private func requireCoverage(_ condition: @autoclosure () -> Bool, _ message: @autoclosure () -> String) -> Bool {\n         guard condition() else { XCTFail(message()); return false }\n         return true\n",
      "untracked": []
    },
    {
      "worktree": "LOCAL_HOME/Developer/sqe-platform-release-layout",
      "HEAD": "40bae2640b23eb05496a093d09a4c20186403879",
      "branch": "refs/heads/codex/mcx19b-release-layout",
      "diff": "",
      "untracked": []
    }
  ],
  "history": "40bae2640b23eb05496a093d09a4c20186403879\t2026-09-17T07:24:46+10:00\tAdd isolated ACE iOS discovery cases and preserve failure evidence\n52b212aca02190ee0e742debe383fa6016fe36b9\t2026-09-15T23:37:24+10:00\tReport unreadable Settings controls with observed state\naf8c82dc2e7e3faec77e03d8d902e1642943f29c\t2026-09-15T23:24:14+10:00\tBound native Settings recovery to two state-checked taps\nc0e53738d15f677f9fd69b4d1eeba9f228ac51d1\t2026-09-15T22:43:47+10:00\tUse explicit numerical precision for native target measurements\nf5bd9c43cb1bf6dfb45986b9e5df420be28ad9d2\t2026-09-15T22:27:29+10:00\tDiagnose exact native target-size measurements\nc8b10875658e5fbd6a66dea1fbcec9bb4a2f5b0d\t2026-09-15T21:54:39+10:00\tCapture complete simulator screens for native coverage evidence\ne4264d27aeff1fc53accf84b39e48249ecb89b5e\t2026-09-15T21:27:28+10:00\tFix scrolling to offscreen controls in native coverage tests\n6718b437b10a51eeee0e5c6b3918dc2d5e052260\t2026-09-15T20:25:20+10:00\ttest: trace copy confirmation audit frames\n15e4363c2ac074ceeea71599d1f2b64f67446487\t2026-09-15T19:45:09+10:00\tfix(ios): clip release scrolling to its viewport\n026dd4bf75c341ec4c4cb2100855b724c60d6f4f\t2026-09-15T19:31:44+10:00\tfix(ios): select the default edge padding overload\nadd02b803eaf9e655f544f9b111733d1c07004b7\t2026-09-15T19:27:37+10:00\tfix(ios): keep release margins inside scroll content\neef4b163726444eb3c7bfd6e936cf9c8b53e46bc\t2026-09-15T18:31:50+10:00\tfix(ios): avoid duplicate simulator shutdown during restoration\n905004fc71107565abf2d68cb05fc581cf5bf310\t2026-09-15T17:49:07+10:00\tRun ACE coverage one required simulator at a time\na84f67010362c9e87e49576af8cf40b2c23a807b\t2026-09-15T16:19:02+10:00\tFix nested orientation waiter cancellation in iOS tests\n464671562cfb0249e859ffc8905a8f8c5ef17e55\t2026-09-15T12:54:03+10:00\tfix(ios): verify native settings navigation and final values\nb2c45093a44c6171ca250b858378ea57693d8477\t2026-09-15T11:05:23+10:00\ttest(ios): bind specification check to approved pilot scope\n493e632b2640fb12b54bf709719b0320cadd899a\t2026-09-15T10:47:02+10:00\tfix(ios): compare stable simulator environment for verification\n023b1e8dd0a742d2a1caa3f30f5141af2c5021db\t2026-09-15T09:41:29+10:00\tfix(ios): simplify release text and use primary control tint\n1b38b90e9a949b7e73965602612c11be6f19e9fc\t2026-09-15T06:28:05+10:00\tSimplify ACE clipboard to complete action records\nb63af743b207fcbb78df1b473707660789a2dc60\t2026-09-14T22:25:59+10:00\tVerify all release values through native clipboard actions\n7fee853aef52e85a813368a51d833c134710186d\t2026-09-14T20:45:40+10:00\tPreserve runner timeout output and correct evidence projections\nb9ba714d99862e9ef5572b7eb2a1b1d6471d0932\t2026-09-14T20:24:16+10:00\tAdd local iOS verification and align fictional pilot screens\n7dc7cee7654ae8cc002da54324c3d1475a8494e6\t2026-09-12T16:37:56+10:00\ttest: name the complete evidence publication contract\n50aaf697673756e7935fcc20908c44749a905e51\t2026-09-12T16:27:12+10:00\ttest: retain private native evidence and capture release rows\n783a12113a8e85ba228d5a96f6bdc2678820e3e6\t2026-09-11T21:38:04+10:00\ttest: align native evidence pin with approved specification\n3acd4147c176465a7c8f167da542d8cbd7184e3a\t2026-09-11T20:06:24+10:00\tfix(ios): size full-matrix retention controls from native records\n0196fdbba0071c53747249dd0352b6f9e36fb555\t2026-09-11T18:49:09+10:00\ttest: retain setup phase in missing-resolution-log fixture\n045e626e9c0e8b91de2869eee8fd8a36dc4f183a\t2026-09-11T18:34:51+10:00\tfix: preserve exact live retention ownership and interruption evidence\na3f6d5e75a7a95c9c533fb46faac6e4562c1cc59\t2026-09-11T17:43:01+10:00\tfeat: retain each full matrix test before advancing\n4b92bc340b2997eaf0200a220b4b1b03eb619900\t2026-09-11T14:48:37+10:00\ttest: bound the manual live evidence workflow check\n3e1415f28c78406f61af6aafb04844e42cc19dab\t2026-09-11T14:40:01+10:00\tfix: preserve diagnostic workflow test boundaries\n2eafeb48a01431563d95aba8949260ef06b11ce5\t2026-09-11T14:23:29+10:00\tfix: retain verified MCX-19 pilot checkpoints and acknowledgements\n39eed65f197c9344216bbcbe938ce0812f5d9d5d\t2026-09-11T09:37:00+10:00\tdiagnose(mcx-19): isolate Pro Max contrast audit\n86d60f395feb22967db94ba360bec9bce056ca21\t2026-09-11T09:19:03+10:00\ttest(mcx-19): align action target source contract\nb33548a53a8808d42888a0aed9dc7dcb64cefa46\t2026-09-11T09:08:27+10:00\tfix(mcx-19): retain baseline history in Codemagic workflows\n1883d95e938dff9afdeaca018472120a22fb3d6e\t2026-09-11T08:45:00+10:00\tfix(mcx-19): repair native checks and add focused verification\n6155d1197dc01910b0d7cd8bbb4ceb949969c5f9\t2026-09-11T05:18:04+10:00\tci(mcx-19): allow live evidence matrix to finish\n809a4576fb03d35eea52d0a2fe174151e3fc2949\t2026-09-10T19:07:53+10:00\tIncrease simulator verification window for live evidence\ndbb2bbf5e27011d0590d3e04b95fbeefd8647ba6\t2026-09-10T14:09:33+10:00\tci(mcx-19): use m2 pool for live simulators\n0fed7141bdc7268b3f86cc3ccf82aa4b901f59d4\t2026-09-10T14:00:45+10:00\tdocs(mcx-19): correct simulator log stream wording\ne25dd29c8aae3da3f5a22088e8f20e66b22aa6c1\t2026-09-10T13:54:23+10:00\tfix(mcx-19): keep simulator diagnostics off destination stdout\n7519dba068ee6c52497c29acfbf6dd263bf764d5\t2026-09-10T13:47:54+10:00\tfix(mcx-19): harden simulator diagnostics\na3a95955c862d55b9bb679fa988fecb4429a59c5\t2026-09-10T11:40:48+10:00\ttest(mcx-19): correct native action order fixture\n2b5cebe2144ad0fc99fa4be748d3653e2a8038ce\t2026-09-10T10:39:32+10:00\tfeat: retain full matrix records in private quarantine\n4c2e56efc4ef21355707ac612e21da5a543fedf9\t2026-09-10T06:49:07+10:00\tfix: retain unresolved native records in private quarantine\nf9dba2f78929eb6ce00fb2e2d4c6b3732dbac12e\t2026-09-10T04:46:43+10:00\tInspect blocked native records with a bounded private decoder\nbcfbbb26d7073d1dc5a21b977a1e40158aaf6b3e\t2026-09-10T04:05:13+10:00\tClassify blocked native record prefixes safely\n06f60eeb3446f45ff7cb5bdf80cb714d51b641bf\t2026-09-09T22:01:57+10:00\tIdentify native content scan failures with fixed safe categories\nba42dd4e5cf9ddf1d9517b069820aae584792321\t2026-09-09T21:45:36+10:00\tRetain native result bundle filenames without changing path limits\na0e146d5faac7db91cc20efd7425ad1129b5d7f1\t2026-09-09T21:29:27+10:00\tReport bounded native archive path diagnostics\n25e978226b98a05a738c79eec91a4a47191d8d6c\t2026-09-09T21:08:58+10:00\tAllow bounded native simulator setup time\n980a25af4edfccd3171ef8d91b9aa6d761e87765\t2026-09-09T20:52:40+10:00\tPrepare MCX19-B native failure evidence cycle\n725d1a0fd7ed945fc59b3912871dbcde267a9ffb\t2026-09-09T15:37:46+10:00\tfix: retain safe iOS setup diagnostics within pilot budget\n33c995a25d7fa4c1103faafa676d310c4d9a6894\t2026-09-09T15:14:27+10:00\tPrepare exact simulators before native evidence checks\n7f2a002926ce0447ad628ccf42516a78281eeaff\t2026-09-09T14:44:05+10:00\tRetain actionable native unit diagnostic evidence\n74c9a08729a4aa5530d59fd700147ab7beab273b\t2026-09-09T13:09:10+10:00\tCapture initial iOS audit state in focused diagnostic\n5929b0cf42cfa7855b451d0d93d088a79e6e99ee\t2026-09-09T12:31:21+10:00\tRetain focused MCX19-B iOS diagnostics\nb494ae95a1324091ce817cfc94694c8fd91cba97\t2026-09-09T10:33:07+10:00\tPrepare MCX19-B native evidence collection\n3259048186916941bf3557d55503e7375e432c57\t2026-09-08T21:28:43+10:00\tci(ios): run manual diagnostic on Mac mini M4\nf9b77a819fd608cb57d334d2a106ea80422222e4\t2026-09-08T19:13:28+10:00\tfix(ios): move sign-in heading into an accessible form row\n3105a63b95aec0b04b36b0181abfc4b94539aa7e\t2026-09-08T14:13:55+10:00\tfix(ios): repair identified accessibility layout issues\n838ae6af5933495c49f74d3e47109e6159bb951b\t2026-09-08T13:14:19+10:00\tfix(ios): remove non-Sendable audit callback capture\n583daded5540d5f87a95138694f37548290ce2e6\t2026-09-08T12:37:44+10:00\tfix(ios): make audit callback capture explicit\n8348d9f0650a50df1c89406f0664ba8b828689bf\t2026-09-08T12:16:00+10:00\ttest(ios): retain bounded accessibility issue details\n0a936811f75713d43235f86176e55e756b48ba73\t2026-09-08T11:29:50+10:00\tfix(ios): size action targets and scope functional diagnostic\nb6eabcdc36e80ba55ff0f6dd1201b96a0547b44e\t2026-09-08T09:04:13+10:00\tFix deterministic UI appearance and isolate its assertion\nd7955042870c5458778bf57c4bccc5703aa0a08b\t2026-09-08T06:54:07+10:00\tfix(ios): read appearance indicator from window view\n1dc21a3481de549f338b34ee298f67c3adb0bc5d\t2026-09-08T06:28:19+10:00\ttest(mcx-19): isolate dark orientation diagnostic\n3a52b52c0e6124137f2f25a9ce810fb489c7adcf\t2026-09-07T20:34:39+10:00\tfix(mcx-19): retain safe progress after each live command\nc8dc459c20472092b80b83d44d980b5670dc61fa\t2026-09-07T17:15:12+10:00\tfix(mcx-19): forward UI input and use unsigned negative build\n18ec5a0456c1392022f27007dab3fd071266b9f7\t2026-09-07T16:01:08+10:00\tfix(mcx-19): isolate diagnostic approval group\nc22c246d71026fa73b0b74cf95bbab8b7a3b1308\t2026-09-07T15:58:13+10:00\ttest(mcx-19): retain two bounded diagnostic errors\n965c244d29b428091879194ed1143ac1fbf95422\t2026-09-07T14:43:08+10:00\tfix(mcx-19): report controlled live failure diagnostics\n40f04d96311d6cc3f384d3afe06eb9329df33859\t2026-09-07T10:58:50+10:00\tfix: repair MCX-19 live Xcode command settings\na1daf3dbde1b72ce05db9f4857d7127848f19c2e\t2026-09-07T09:47:45+10:00\tMCX-19 repair live simulator setup gate\n3703458db72ce1e388967c7af3b760108db3bab9\t2026-09-07T08:05:29+10:00\tMCX-19 add controlled live failure reasons\n7e663758df36a1c54f9f7a18d0f056242978175e\t2026-09-06T18:24:56+10:00\tfix: report failed live evidence commands\nfcbe3583b54a974e74eb71d4175a01e587c9a79c\t2026-09-06T15:05:41+10:00\tfix: allow live UI appearance input\n4cf12fd5395fc290235e359aa8485f903b8f11da\t2026-09-06T14:05:41+10:00\tAccept Codemagic authenticated repository remote\n11b41a2631831361bd67b6d5a03c3d32424e1186\t2026-09-06T11:21:13+10:00\tClarify MCX-19 approved input handling\nec0f19bc1e469ba90d9bb90f9b71fdaf3b7d401a\t2026-09-06T11:09:12+10:00\tImport MCX-19 Codemagic variable group\nf77bc6184a04fe1abab4574070a18cc807adf9c2\t2026-09-06T09:47:03+10:00\tEnforce MCX-19 manual trigger controls\nacea7785df09207e4cda3600499357c1b12c7234\t2026-09-06T09:33:42+10:00\tHarden MCX-19 approval boundary\nc78dfc5c717eafe46c37afade7eca4e7e1c5d044\t2026-09-06T08:43:01+10:00\tUse canonical macOS evidence path\n7fa7df8fdf80c65485d690c8262fada8f6665cda\t2026-09-06T08:30:49+10:00\tAdd MCX-19 live evidence harness\n7da6228dc87ad970aa8d44365fbc3823c58020da\t2026-09-06T08:01:55+10:00\tMerge pull request #5 from mcxl/codex/mcx-18-strict-ui-test\n6d5a20c51ec009cc0a7f317f96e4095f35820c34\t2026-09-06T07:19:26+10:00\tAvoid absolute path interpolation in UI Make target\n4306edd463271c8560225f0369b16fccc31a29bf\t2026-09-06T07:04:12+10:00\tExercise real resolver in UI Make test\n61a1cc58a6a38d09e93c3205d51da637c381d233\t2026-09-06T06:23:40+10:00\tTest strict iOS UI Make command path\n721bbf34e9efa644f162c8b8cfcabb0712356c22\t2026-09-05T21:44:37+10:00\tMerge pull request #4 from mcxl/codex/mcx-21-align-reviewed-evidence\n3de745934d62034d843032ad9b202b2fecc93a6b\t2026-09-05T20:58:26+10:00\tTest blank reviewed evidence fields\nc345b4e5654be0efc4d767e5621618ecfc06122d\t2026-09-05T20:51:30+10:00\tAlign reviewed evidence field description\n2140dc9382ded3aabb3969676dcb251fbb9d4441\t2026-09-05T20:04:46+10:00\tMerge pull request #3 from mcxl/codex/mcx-22-wrong-uuid-simulator-regression\n2e0e31ffe21feee4ce3e5927c733d70e429429b9\t2026-09-05T15:49:23+10:00\tAdd wrong UUID simulator regression\n80bbe4b4e41a03c381c7ada59cbae317b37c4992\t2026-09-05T15:30:54+10:00\tMerge pull request #2 from mcxl/codex/mcx-20-mate-origin-cleanup\n25a3035eed07306e5b3e4aa8a67d8aa2235fed34\t2026-09-05T14:22:34+10:00\tProtect MATE origin cleanup from races\na40d34b82d784baee498361a837c3585cbab48d9\t2026-09-05T14:08:10+10:00\tRemove dead MATE origin records\n92cd41966e7a8fa5eabdad94c77b30c818e21be4\t2026-09-05T13:51:41+10:00\tMerge pull request #1 from mcxl/codex/sqe-pivot-gate-bootstrap\nbdbceb6d0dd260dded1782f9b9c76aa5f7bad14b\t2026-09-05T11:48:05+10:00\tFail closed on simulator ambiguity and evidence types\n7db3dd7a783120401575206f838d1f34ceabc2dd\t2026-09-05T10:58:39+10:00\tHarden public evidence preflight controls\n38c1790ff83b26ec49a8f261ee57ff5103139ab2\t2026-09-05T08:59:50+10:00\tClose Pocock approval and evidence gates\n6dbf8046858df7c6e25d2dbef7db73b8d9d303ce\t2026-09-05T04:06:14+10:00\tClose remaining Pocock release gates\n183ae36a464e7b46a6f424551d4ba9eae8c50881\t2026-09-04T22:41:06+10:00\tProvision required iOS simulators\ne6560a496a4babdd764bb18890402d90d9ecf44b\t2026-09-04T22:24:12+10:00\tPreserve adversarial trace gate tests\neaa383f09566a37b0cbeb2ed3ba9dfeaa8cfafac\t2026-09-04T22:15:18+10:00\tFix Pocock release blockers\n5af22fa37df865616ebfb6549b1dbf39cfd460de\t2026-09-04T21:22:13+10:00\tHarden quality workflow triggers\n8a6742fe5a6730cd66a9985a642bfc5743fd59f2\t2026-09-04T20:57:28+10:00\tEnable manual quality gate dispatch\nbfa9013ed219a47ed5e14c19415d88104a227210\t2026-09-04T20:55:38+10:00\tFix controlled iOS runner contracts\n326b2b7f5292ac71423201f32e6b68222e0e7e25\t2026-09-04T20:24:30+10:00\tModel Windows LibreOffice probe platform\n308f12fd1369b55ac03c7b2633f5a4f71372db39\t2026-09-04T20:18:55+10:00\tInstall LibreOffice for Python quality gates\n9414b265863d4ba4989ac7b5b4ca8d2cd8652e49\t2026-09-04T20:02:53+10:00\tRun Python core without document runtime\nfe91ef14d1eef0b7c441b5551bca5cacb3ad7934\t2026-09-04T20:00:37+10:00\tFix Python quality gate bootstrap\nf0b9da8e6d89da2d632fbb2e106f84ce349e2c97\t2026-09-04T19:55:17+10:00\tAdd core and release quality gates\n59378e9277bd4c7c9be89694f2220cbc28e7c211\t2026-09-04T19:48:46+10:00\tAdd tiered SQE platform test runner\n01d583af137ca645ce830e3e5e26fc8e1bd7c036\t2026-09-04T19:36:23+10:00\tOrganise controlled SQE technical documentation\n6e0e66ea78cc48fcd10a34d4a30220506aebcb9d\t2026-09-04T19:29:09+10:00\tSupport Windows LibreOffice discovery and guarded trace fixtures\nc761fe2045e9f16dc1bd1770f8d940215247e26d\t2026-09-04T18:27:41+10:00\tPort MATE approval construction guard\nd1507336ca6be5f6ae5f90227a66f10dffa83728\t2026-09-04T18:22:50+10:00\tImport controlled ACE iOS source snapshot\n1084f13a041ff3866daf5fc9e68d17ce173fb3e5\t2026-09-04T18:21:47+10:00\tInitial sanitised SQE platform baseline\n",
  "refs": "7dc7cee7654ae8cc002da54324c3d1475a8494e6 refs/heads/codex/mcx-19-live-evidence-harness\n40bae2640b23eb05496a093d09a4c20186403879 refs/heads/codex/mcx19b-release-layout\n7dc7cee7654ae8cc002da54324c3d1475a8494e6 refs/remotes/origin/codex/mcx-19-live-evidence-harness\n"
}