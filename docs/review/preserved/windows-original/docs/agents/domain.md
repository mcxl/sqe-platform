# Domain Documents

Use the root `CONTEXT.md` as the controlled vocabulary guide.

Use `docs/adr/` for future architecture decisions.

Use `workflows/` for approved auditor workflow specifications.

Use `docs/specs/` for approved build specifications.

Use `WORKFLOW-NOTES.md` for approved workflow design decisions.

Use `ACE_VISION_AND_ROADMAP.md` for future phases and deferred options.
